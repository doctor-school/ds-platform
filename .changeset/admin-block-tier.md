---
"@ds/design-system": minor
---

Add the admin block tier to `@ds/design-system`: `data-table` (two-line record rows on desktop, stacked record cards under 768px, optional whole-row activation with an owner-approved `tint-pressed` state), `filter-bar` (instant/debounced apply with removable chips), `combobox` (closed RU vocabulary with per-option explanations), the form section family (`FormSection`/`FormFieldGroup`/`FormSeparator`/`FormActions`/`FormDerivedNote`), `pagination` (numbered pages) and `empty-state` (no-data vs no-results variants). All adopted from the committable registry whitelist and re-skinned to tokens.
