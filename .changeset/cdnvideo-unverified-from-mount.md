---
"@ds/room": minor
---

006 EARS-18: a structurally silent player provider (cdnvideo) now mounts in the `unverified` state and the room renders nothing of its own over it — no watchdog advisory banner, no covering overlay and no restart control, just the bare provider iframe whose own in-iframe controls are the only affordance.
