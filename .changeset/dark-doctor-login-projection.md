---
"@ds/doctor": patch
---

The doctor sign-in door now speaks for itself: the return-context panel promises the return happens on sign-in (not after an email confirmation, which is the registration door's step), the wordmark follows the chosen theme instead of vanishing into the dark page, and a doctor who already holds a session is sent to their landing instead of being shown a password box.
