---
"@ds/portal": patch
---

The /login, /register and /reset page titles now render as a real `h1` heading (exactly one per page) instead of a plain `div`, so screen readers and assistive tech see the page structure. Purely semantic — the visual rendering is unchanged.
