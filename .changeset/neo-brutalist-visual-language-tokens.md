---
"@ds/design-system": minor
---

Codify the neo-brutalist visual language into the DTCG token SoT. Structural repaint of the design tokens: **radius 0** (flat, non-rounded system — the Tailwind `rounded-*` ladder collapses onto `--radius-control`), **hard offset shadows** (blur-0, theme-aware cast tones via the new `elevation`/`elevation-soft` roles), a **hard structural `border`** (near-black outline) split from a subtle `hairline` divider, the **amber `warning`** family with dark-ink foreground (white fails AA on amber), an expanded type scale (kegel 10–56) with an `extrabold` (800) weight, role-named letter-spacing, and a `micro-label` eyebrow composite. The AA action-fill triad (`primary-action`/`primary-hover`/`primary-pressed`) and brand `primary` anchor are preserved. Tokens only — no consumer wired.
