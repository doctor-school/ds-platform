---
"@ds/api": patch
---

Add explicit Postbox SMTP selection with bounded, cancellable email attempts and opt-in fallback. Keep uncertain acceptance from triggering duplicate sends, and report provider acceptance separately from delivery.
