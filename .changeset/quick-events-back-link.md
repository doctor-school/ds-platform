---
"@ds/admin": patch
---

Add a one-click «← К списку мероприятий» back-to-list affordance to every inner
admin screen (create + edit), so the operator is never stuck in a navigation
dead-end from the event page (Stage-B feedback on #660). Adopts the owned
`@ds/design-system` `Link` primitive (token-only, blue.700), copy via the RU
catalog.
