---
---

#2274 — staging slot environment regeneration retains the API Mailpit host and
port alongside its slot-tagged sender. The existing live slots were repaired by
API-only recreation while preserving their images and databases.

This empty-package release record covers staging operator tooling only and bumps
no workspace package.
