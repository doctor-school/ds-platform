# DEBT.md — rolling debt ledger

**What belongs here:** below-threshold debt — deviations, deferrals, and cleanup items that do NOT warrant a GitHub Issue. The **significance threshold** (canon: AGENTS.md §6): a tracker Issue is filed ONLY when the debt (a) blocks / sits on the critical path of a product deliverable, (b) is user-visible or a prod risk (security/data), (c) must be acted on before the next release, or (d) is a gap in a CI guard that gates other PRs (a blind gate cannot gate). Everything below that threshold is one line here, appended in the **same commit or same PR** as the work that surfaced it.

**Scope boundary (owner-directed, 2026-07-16):** the threshold routes debt **at surfacing time** (wrap / `surface-decision-debt` / opening Issues from a spec). It is NOT a mandate to re-grade or close already-filed Issues — re-triage of tracked Issues happens only on an explicit owner request; a drainage session **implements**, it does not prune (precedent: issue #881, 2026-07-16 comment).

**Owner triage cadence:** weekly and at each milestone close.

**Promotion:** when a line crosses the threshold, file an Issue via `pnpm issue:create` with exactly one `source:*` label (`source:owner` | `source:spec` | `source:retro` | `source:agent`), exactly one kind label, exactly one `track:*` label (`track:academy` | `track:doctor` | `track:platform`), and a `--milestone` («Platform ops & hardening» when no product theme fits), then check the line off with the Issue # appended.

**Line format:**

```
- [ ] YYYY-MM-DD <origin: session/PR/Issue> — <one-line debt> (promote-when: <criterion>)
```

## Ledger

- [ ] 2026-07-16 Issue #701 / open-ears-issues — child #1049 groups EARS-15..16 in one `kind:ears-handler` (same events read-module + `packages/schemas/` touch set, not parallelizable; open-ears-issues 3b deviation from 1:1) (promote-when: the grouped child stalls and the EARS need separate owners)
- [ ] 2026-07-16 Issue #701 / open-ears-issues — child #1051 groups EARS-17..18 in one `kind:ears-handler` (both are interaction controls over the same listing/month-view component touch set; open-ears-issues 3b deviation from 1:1) (promote-when: the grouped child stalls and the EARS need separate owners)
- [x] 2026-07-16 retro session 1c4b7478 / PR #1011 — AGENTS.md at 25,596/25,600 B (4 B headroom); the next always-on edit forces ad-hoc squeezing of canonical rules (promote-when: an AGENTS.md edit does not fit the budget, or a drainage wave picks it up — remedy: add a `<256 B remaining` WARN tier to `tools/lint/instruction-budget-lint.ts` + relocate §6 detail to `.claude/rules/`/skills) (→ #1042 — WARN tier shipped; relocation half satisfied by #1030)
- [ ] 2026-07-16 Issue #1046 / mailer failover — `bff_mailer_relay_events_total` registers in the prom-client default registry but the api exposes no /metrics scrape endpoint yet (no Prometheus server exists on the stand/prod either — engineering-readiness default, unbuilt); GlitchTip events + structured logs are the live-visible EARS-32 surface today (promote-when: the engineering-readiness Prometheus slice lands — add the exposition endpoint with it)
- [ ] 2026-07-17 spec-010 WBS (#1086) — EARS grouped away from the 1:1 default per open-ears-issues §3b: #1087 folds EARS-1,2,4,6,7 (one trigger-function+migration touch set), #1088 folds EARS-3,5 (one tx-wrapper touch set); bodies list folded ids (promote-when: a folded EARS needs independent scheduling)
- [ ] 2026-07-17 PR #1096 (#1088) — background/system writes (auth user-mirror reconcile; future jobs) carry no request context and land as source='db-direct' instead of design-§3 'system:<job-name>'; wire withAuditContext({source:'system:<job>'}) at job entry points (promote-when: a background job mutates domain tables in prod or an investigation needs job-level attribution)
- [ ] 2026-07-17 Issue #1103 primitives-shell — 15 call sites (#1103 ledger @ ed20987d) use DS primitives as shells with call-site visual-identity overrides across portal/admin (worst: app-shell-header 4, month-calendar-view 4); kept continuously visible by the `primitives-first` SHELL WARNs, drain opportunistically per surface, no dedicated Issue (§6 threshold: not user-visible defects). RAW(3) routed to #1107 (promote-when: a surface's shells are remediated as a batch or the guard flips to BLOCK)
- [x] 2026-07-17 PR #1108 (#1103) (→ #1253) — the `continue-on-error: true` WARN-job pattern in ci.yml (aa-contrast, instruction-budget, form-error, form-rhythm, submit-pending, interaction-states, …) keeps the WORKFLOW green but the CHECK-RUN still concludes FAILURE the moment a WARN guard emits findings, which `merge:gate` treats as blocking → would block EVERY PR's merge gate; latent today only because those guards are all zero-findings. `primitives-first` fixed here (WARN moved into the tool exit code, job de-`continue-on-error`d); sweep the rest the same way when one fires or at the ADR-0007 §2.6 severity review (promote-when: a `continue-on-error` WARN guard starts emitting findings, or the §2.6 review runs)
- [x] 2026-07-23 PR #1153/#1121 live-verify — portal e2e `room-display-name.spec.ts` + `room-access-branches.spec.ts` desktop asserts on `room-context` testid are stale after #1123 made it a mobile-only tab (promote-when: room e2e suite gates CI or next 006 iteration touches them) (→ #1254 — the desktop assertions now target `room-context-strip`, the face `WebinarRoomLayout` actually mounts on desktop; `expectNoRoom` asserts BOTH faces)
- [ ] 2026-07-24 PR #1159 (#1141) — presence-count publisher state (last-published latch + expiry timer) is in-memory per api instance; a 2nd instance would emit idempotent duplicate publications (never a wrong count) — Phase-0 single-instance scope, recorded in the service doc (promote-when: api scales past one instance — move the latch/timer to a shared store or leader election)
- [ ] 2026-07-24 PR #1157 (#1158) — `spec-link` BLOCK guard has no docs/spec-revision linkage form: a spec-first slice PR must not `Closes` the parent impl Issue, so a sub-issue was minted (#1158 precedent) solely to satisfy the guard; document the pattern in repo-conventions or add a guard carve-out (promote-when: the next spec-first slice hits the same wall)
- [x] 2026-07-24 PR #1163 live-verify — `apps/portal` room-axe.e2e.spec.ts (dev-stand-gated, CI-skipped) asserts the MOBILE `room-context` testid on Desktop Chrome — precondition fails before any axe scan; stale selector predates #1163 (desktop/mobile split unchanged since the #1123 rework) (promote-when: the live-axe suite is wired into a CI gate or the next dev-stand axe sweep) (→ #1254 — precondition now asserts `room-context-strip`, so the scan is reached on Desktop Chrome)
- [ ] 2026-08-03 PR #1170 (#1169) — the screenshot guard steers writes into `<repo>/.playwright-mcp/`, which is git-ignored but still IN the working tree and unbounded: it had grown to 8.8 MB (63 PNGs + 270 page snapshots + 78 console logs, 2026-07-17…24) before this session emptied it by hand. The server writes there by default (`outputDir()` = `path.join(cwd, ".playwright-mcp")`) and only leaves the tree if launched with an explicit `--output-dir`, which the Claude-Code playwright plugin does not pass RESEARCHED 2026-08-14 (#1254), still open: the output dir is NOT settable from repo-level config. The server is supplied by the Claude-Code `playwright` PLUGIN, whose launch definition is `{"command":"npx","args":["@playwright/mcp@latest"]}` in the marketplace clone under `~/.claude/plugins/marketplaces/claude-plugins-official/external_plugins/playwright/.mcp.json` — outside the repo, per-machine, and with no `--output-dir`. This repo carries no `.mcp.json`, and `.claude/settings.json` has no `mcpServers` key to override a plugin server's args; adding a repo `.mcp.json` would register a SECOND competing playwright server rather than reconfigure the plugin's. So the cause cannot be removed from this repo, and no wrapper/cleanup-cron was invented for it (AGENTS.md §6 — no workarounds) (promote-when: a `pnpm repo:hygiene` sweep is built — owner deferred it on 2026-08-03 — and takes a bounded `.playwright-mcp/` prune step; or the plugin gains a settable output dir; or the dir grows large enough to slow a tree scan / show up in a backup)
- [x] 2026-08-04 Issue #1139 / live-verify — `pnpm dev:db:branch --help` treats `--help` as a branch slug, creates and migrates `ds_dev___help`, and takes a dataset-global pre-migration snapshot instead of showing usage; add option parsing or refuse leading-dash branch args (promote-when: the next dev-stand CLI/DX hardening slice runs or another session triggers unintended state) (→ #1254 — `--help`/`-h` prints usage and exits 0 touching nothing; every leading-dash argument is refused by `branchDbName`, covering db-drop too)
- [ ] 2026-08-07 spec 011 WBS (#1189–#1195) — open-ears-issues 3b grouping: 12 EARS folded into 5 handler Issues by intersecting touch set (EARS-1..3,10 session module; 4..5 enrollment; 6..7 challenge; 9,11 cross-flow suites) instead of the 1-EARS-1-Issue default; folded ids listed in each body, sequencing via native blocked_by chain (promote-when: a grouped Issue proves too large in flight — split at dispatch time, not by re-cutting the WBS)
- [ ] 2026-08-07 PR #1199 (#1189) — provision.sh step 9 (MFA login-policy converge) verified only as an extracted run against the live stand; the full script end-to-end on a FRESH stand was not exercised (script header bans Windows execution). Step 9 is verbatim in the script with the real api/api_idempotent helpers (promote-when: the next fresh dev-stand provisioning runs — verify the `mfa sweep:` verdict lines appear and the EARS-8 suite is green from scratch)
- [x] 2026-08-10 PR #1207 (#1191) — the audit-emission registry cannot express "route owes NO durable row": `emits: []` is rejected and `deferred` semantically means a tracked gap, so `POST /v1/admin/auth/mfa/enroll/start` had to classify `audit: low-stakes` to state the 011 Event Model's "emits nothing durable" (promote-when: a second no-row route hits the same wall — add an explicit `none-by-spec` marker to the registry schema + guard) (→ PR #1271 — `noneBySpec: { reason, spec }` added to the registry schema beside `emits`/`deferred`, validated by a shared pure validator; `mfa/enroll/start` reclassified from the stand-in `low-stakes` to `high-stakes` + a cited none-by-spec entry; a bare `emits: []` still rejected)
- [ ] 2026-08-11 PR #1210 (#1192) — the 011 EARS-7 admin second-factor lockout keeps its counters in an in-process `Map` (`apps/api/src/auth/admin-session/mfa-lockout.service.ts`): an api restart DROPS every live lock, and a second instance would fragment the per-subject tally so each replica enforces its own 10/30-min budget. The Redis rebinding seam is named in the service doc-comment (same shape as `RateLimitService`/`SESSION_STORE`), so the fix is a binding change, not a redesign; acceptable while prod api is single-instance (promote-when: prod api scales past one instance, or a restart-clears-the-lock incident is observed — back the counter with the same Redis the session stores use)
- [x] 2026-08-11 PR #1193 brief-A live-run, extended by PR #1218 (#1194) — THREE audit-row window assertions fail on a REMOTE-Postgres dev stand only (green in CI): `apps/api/test/auth/mfa-enroll.e2e-spec.ts` EARS-5.4, `apps/api/test/auth/mfa-challenge.e2e-spec.ts` EARS-7.3, and `apps/api/test/auth/password-reset.e2e-spec.ts` EARS-35. All three fence the row window with a test-process `new Date()` while the stand's Postgres stamps `created_at` from its OWN clock, so whenever that clock runs behind the test host the row the test just produced falls outside its own window and the assertion reads `[]` (→ #1219 — fence confirmed as the root cause and branch-DB audit partitioning excluded by evidence; every `new Date()` audit fence under `apps/api/test/` now reads its lower bound from `dbNow()` like the sibling suites, five sites across four files including `registration/idempotency.e2e-spec.ts`, so the assertions no longer depend on the sign of the host↔DB skew)
- [ ] 2026-08-11 PR #1218 (#1194) — `ZitadelIdpClient.getProjectRoles` reads user grants through `POST /management/v1/users/grants/_search`, a surface the #203 note records as deprecated wholesale in the v4.15 proto (which is why the WRITE moved to `zitadel.authorization.v2.AuthorizationService/CreateAuthorization`). Its read counterpart `ListAuthorizations` IS routed on the stand (it answers `400 invalid_argument`, not 404) but rejected every `AuthorizationsSearchFilter` field name probed in #1194, so migrating needs a proto-verified filter shape plus its own live proof. The v1 shape is proven live and pinned by `zitadel.idp.spec.ts` in the meantime; the same migration covers the adapter's other management-v1 reads (`hasTotpFactor`, `resolveOrgId`) (promote-when: a Zitadel upgrade drops a management-v1 route the adapter uses, or the next auth-adapter slice can carry the v2 filter-shape probe)
- [ ] 2026-08-11 PR #1218 (#1194) live-run — every mount of `/mfa/enroll` registers a FRESH provisional TOTP factor and silently supersedes the secret already on screen (011 design: the offer is shown exactly once and is not re-servable). Correct as a security property, but an operator who reloads or opens the page in a second tab mid-transcription loses the secret they were copying with no message — the browser journey had to reload-and-re-read to work with it. Product call, not a code fix: warn on remount, or keep the offer stable for the pending reference's lifetime (promote-when: raised at the #1194 Stage-B gate — the owner rules on it there)
- [ ] 2026-08-14 PR #1250 (#1249) — `.lycheeignore` now excludes the whole `primer.style` domain, suppressing the link check on the three GitHub Primer citations in `design/constitution.md` (button states, form errors, segmented control). The cause is FLAKINESS, not a clean outage: three CI runs on this branch on 2026-08-14, with no change to the tree between them, gave `markdown-links` red, red, green — the reds reporting three `Timeouts` / zero `Errors` on the same three URLs, while residential probes hung on both `/product/` pages across the whole window including at the moment the green run passed (`/ui-patterns/forms/` also 502'd and the domain root hung earlier in the window). Primer's own redirect from `/components/button` targets the hanging canonical URL, so the citations are not stale; they were green in CI from 2026-07-01 (f4c46f72, PR #415) until this date. The URLs in the doc are deliberately UNCHANGED so the sources stay clickable (promote-when: primer.style is confirmed CONSISTENTLY reachable from GitHub-hosted runners across several consecutive runs — never on a single green, which this incident has already shown to be meaningless — then drop the entry and watch a few full `markdown-links` runs before considering it settled)
- [x] 2026-08-14 PR #1250 (#1249) — the `.lycheeignore` entry `^https://github\.com/sidorovanthon$` outlived its recorded cause: the intermittent HTTP/2 protocol error was attributed to the `bbm-ci` pool's squid/slirp4netns egress path, and that pool is gone with the revert to GitHub-hosted runners. The exclusion was deliberately KEPT in this PR — there is no hosted-runner evidence either way, and dropping it blind risks a fresh red `markdown-links` on a boilerplate changesets attribution link (it appears in every CHANGELOG.md entry, so a flake is repo-wide). Comment rewritten in place to record the retired cause (promote-when: a few clean hosted `markdown-links` runs exist to re-test against — drop the entry, watch one full run, and if the link passes remove the exclusion for good; if it flakes on hosted too, replace the cause with the real one) (→ #1254 — three clean hosted `guards-block` runs on `main` (b7c7546, bde2521b, 61e4f073) gave the comparison baseline; entry dropped, full `markdown-links` run watched on this PR)
- [x] 2026-08-13 PR #1237 (#1235) — 31 SEVERITY/header docstrings in tools/lint/*-lint.ts name their deleted standalone CI jobs (e.g. epic-autoclose-lint.ts:52-58, spec-deletion-lint.ts:39-44); guards now run as steps of the guards-block/guards-warn/pr-body-guards batches, so promotion also means moving the step and dropping its WARN-report row (promote-when: the next touch of any affected tools/lint file — fix that file's docstring in the same PR; or a batch sweep if a topology change renames the batches again) (→ #1254 — batch sweep: 44 replacements across all 31 files, each docstring now naming the `guards-block`/`guards-warn`/`pr-body-guards` batch its guard is a STEP of, and every WARN→BLOCK promotion note now also naming the batch WARN-report row. The two RUNTIME message strings that still said "CI job" — spec-deletion-lint.ts, tdd-signal-lint.ts — were fixed in #1253, which also gave them the exit-code contract: step `continue-on-error` + a report step that exits 0 ⇒ green check-run ⇒ does not block)
- [ ] 2026-08-13 #1240 discovery — `author-design-mockup` step 2 prescribes pushing design-system components to the Claude Design canvas "via the `/design-sync` skill", but no such skill exists in `apps/docs/content/skills/`; the lead pushed 6 as-is canvases manually (DesignSync `as-is/`), and the component preview cards of `@ds/design-system` remain un-pushed. Resolve by authoring the `design-sync` skill (or rewriting the `author-design-mockup` reference) plus pushing the component library (promote-when: the next Stage-A mockup run needs the component library on the canvas, or another session hits the missing skill reference)
- [ ] 2026-08-14 Issue #1255 — `typescript` deliberately STAYS pinned `^6.0.3` in all 11 manifests; TypeScript 7.0.2 (the native Go port, published 2026-07-08) is not adoptable yet. `typescript-eslint` / `@typescript-eslint/parser` have no TS-7 build in ANY published channel — `latest` 8.67.0 and `canary` 8.67.1-alpha.4 both declare `typescript: ">=4.8.4 <6.1.0"` — and the ABI backs the declared range rather than contradicting it: `@typescript-eslint/typescript-estree` does `require("typescript")`, resolves `typescript/lib/tsserverlibrary`, and calls `ts.SyntaxKind` (65 sites), `ts.sys`, `ts.createProgram`, `ts.ScriptTarget`, `ts.createSourceFile`, while TS 7's `"."` export is only `{ version, versionMajorMinor }` and both `typescript/lib/typescript.js` and `typescript/lib/tsserverlibrary` fail to resolve (`ERR_PACKAGE_PATH_NOT_EXPORTED`) — the whole classic compiler API moved behind `./unstable/*`. `next@16.3.0` is a second, independent blocker on the same package-layout change (`dist/lib/verify-typescript-setup.js` resolves `typescript/lib/typescript.js`; the fix vercel/next.js#95572 is still open). The `tsc` CLI itself is unaffected — TS 7's bin is a shim that execs the platform Go binary — so the blocker is strictly the JS-API consumers, not compilation (promote-when: typescript-eslint ships a release whose `typescript` peer admits `>=7` — tracking typescript-eslint#10940 — AND Next.js ships the package-layout fix vercel/next.js#95572; then re-run this matrix and bump all 11 manifests in ONE commit, never partially — a split leaves two compilers resolving in one lockfile. The re-test MUST also check what `nest build` actually invokes, not just what the manifests pin: `apps/api`'s build script is `nest build`, and `@nestjs/cli@11.0.24` carries its OWN `dependencies.typescript` at `5.9.3`, physically installed nested under `node_modules/.pnpm/@nestjs+cli@11.0.24_*/node_modules/typescript` — but that copy is INSTALLED, not LOADED: nest-cli's `TypeScriptBinaryLoader` resolves `require.resolve("typescript", { paths: [process.cwd(), ...] })` cwd-FIRST and `nest build` runs with cwd = `apps/api`, which pins `typescript` itself — so the api has always compiled through the workspace pin, and #1258 removed the stale nested copy outright via `pnpm.overrides.typescript`. The re-test point stands in corrected form: verify what the build RESOLVES (`pnpm lint:tsc-version`), never what the manifests pin, so a post-unblock bump cannot report green on a build that never used TS 7)
- [ ] 2026-08-14 Issue #1255 — Dependabot structurally cannot report a major that no sibling's peer range admits, so `typescript` 6→7 was never offered across the 07-13/07-20/07-27/08-03/08-10 weekly runs and will not be while `typescript-eslint` pins `<6.1.0`: its npm updater drops an update whose resolution violates a peer constraint (reproduced locally — `npm install typescript@7.0.2` against a `typescript` + `typescript-eslint@8` manifest ERESOLVEs on exactly that range). `.github/dependabot.yml` is NOT the cause and was deliberately left unchanged — `directory: /` does traverse the pnpm workspace manifests (`ioredis` lives only in `apps/api`, `@refinedev/*` only in `apps/admin`, `jsdom` only in `apps/portal` + `packages/design-system`, and every one of them was offered its own major PR), there is no `ignore` rule, and `open-pull-requests-limit: 5` was never reached (0 open at the time of the check). The residue is a visibility gap, not a config defect: no Dependabot setting surfaces a peer-blocked major, so today the only detector is someone running `pnpm outdated -r` by hand (promote-when: a SECOND peer-blocked major is found sitting unnoticed — then add a scheduled drift report that diffs `pnpm outdated -r` majors against the Dependabot PR history, rather than hand-tuning the Dependabot config it cannot express)
- [ ] 2026-08-14 PR #1260 (#1254) — the MOBILE `room-context` face of `WebinarRoomLayout` (the «О эфире» tab, mobile-only since #1123) has NO browser-e2e cover: every Playwright project in `apps/portal` is Desktop Chrome (`playwright.config.ts` `e2e`/`bdd`, `playwright.axe.config.ts` `chromium`), so no run ever mounts it. The coverage delta from #1260 is ZERO — that PR did not remove mobile cover, it made an already-existing zero VISIBLE by retargeting the desktop assertions onto `room-context-strip`, the element desktop actually renders; before it, the specs named `room-context` but failed their precondition on desktop and covered neither face (promote-when: a mobile Playwright project is added to `apps/portal`, or the next 006 room iteration touches the mobile room surface — then assert the mobile face in that project)
- [x] 2026-08-14 PR #1257 (#1253) — the CI-wiring YAML sketches in the ADR-0006 design spec (`0006-documentation-and-ssot-design-{en,ru}.md`, the `glossary-ids` job) and the ADR-0008 design spec (`0008-repo-strategy-and-dev-workflow-design-{en,ru}.md`, five `continue-on-error: true # WARN v1` jobs + the "severity transition = remove `continue-on-error` + add job to `ci-result.needs`" line) teach JOB-level `continue-on-error` as the WARN wiring. Not merely stale: that shape is the one #1253 removed, because it leaves the CHECK-RUN concluding FAILURE on a WARN finding — a reader who copies a snippet reintroduces the bug that blocked every PR's merge gate. The live contract is per-STEP `continue-on-error` + a batch report step that exits 0 (ADR-0007 §2.6; enforced by `tools/lint/guard-tests/workflow-warn-posture.spec.ts`). Zero job-level `continue-on-error` in `.github/workflows/` today (promote-when: `grep -rn '^    continue-on-error' .github/workflows/*.yml` returns a row — someone followed a snippet — or either design spec is edited for any reason) (→ PR #1271 — all four design-spec files now teach the per-STEP `continue-on-error` + exit-0 WARN-report contract, and the severity-transition line says «move the STEP into `guards-block`», never job-level; live workflows untouched, still zero job-level rows)
- [ ] 2026-08-15 PR #1267 (#1258) — `tsc-version` ships WARN v1 per ADR-0007 §2.6, but its subject is the compiler that produces the PROD artifact of a live medical platform, so a finding it reports is not the kind that should stay advisory for long. Its exit-code contract is already BLOCK-ready (per-step `continue-on-error` + a report step that exits 0), and it is green on `main` today (promote-when: the guard has run green across a few weeks of PRs, or it fires once for real — then move the step from the `guards-warn` batch to `guards-block` and drop its WARN-report row)
- [ ] 2026-08-15 Issue #1268 — `markdown-links` no longer checks this repo's OWN `github.com/doctor-school/ds-platform/…` URLs (1179 occurrences / 433 unique: every changesets-generated CHANGELOG commit+PR link, plus Issue/PR/milestone citations across specs, ADRs, READMEs, DEBT.md). Not a false-positive suppression — none of those links is known to be wrong; the exclusion buys DETERMINISM. «They all resolve» was never established either: lychee v0.24.2's `check_github` short-circuits a PRIVATE repository (`if let Some(true) = repo.private { return Status::Ok(StatusCode::OK) }`), returning OK as soon as the API confirms the REPO exists, without resolving the Issue/PR/commit the URL names. The repo is private, so lychee's plain request 404s them in every run and they are only rescued by a per-run burst of ~433 authenticated GitHub-API calls, drawn from a shared hourly budget (`GITHUB_TOKEN`: 1000 req/h/repo) that every other CI run and every agent `gh` call also spends; when that fallback fails lychee v0.24.2 discards the API error and reports the original 404, so budget exhaustion is indistinguishable from ~1100 broken links (full mechanism + rejected alternatives: the entry's comment in `.lycheeignore`). The residual gap — a genuinely wrong self-reference, a typo'd Issue number, a commit SHA that never existed — was ALREADY uncaught before the exclusion (see the short-circuit above); the exclusion did not create it, it made it visible. The 488 commit URLs are the cheap half to recover: a full checkout (`fetch-depth: 0`, which `guards-block` already does) can verify them offline with `git cat-file -t <sha>`, zero network and zero budget; Issue/PR/milestone numbers have no offline oracle and would need the same API budget (promote-when: a wrong self-reference is actually found in a doc, or the next CI-guard slice has room — then add a local `self-links` guard covering the commit-SHA half via `git cat-file`, and leave the Issue/PR half unchecked rather than reintroduce the network dependency)
- [ ] 2026-08-17 PR #1356 (#1283) — `packages/design-system/src/primitives/textarea.tsx` carries a `form-rhythm-ok:` suppression. The guard's rule A (no `min-h-*` reserved height on a form-MESSAGE element) matches the TEXTAREA's own `min-h-24` minimum height, because `isMessageTag()` classifies any tag whose class string contains `text-muted-foreground` as a message element and the control's class string carries `disabled:text-muted-foreground`. The suppressed line is a control height, not a reserved message slot: the counter span underneath reserves no height and renders only when `showCounter` + `maxLength` are both given (asserted in `textarea.test.tsx`). Below the §6 significance threshold — a guard-heuristic false positive with a recorded reason, no product deliverable blocked, no user-visible or prod risk (promote-when: a SECOND primitive trips the same false positive — then narrow `isMessageTag()` to the enclosing tag's OWN state-independent classes, i.e. ignore `disabled:`/`aria-invalid:`-prefixed utilities, instead of suppressing per file)
- [ ] 2026-08-17 PR #1356 (#1283) — every dependency in `apps/api/src/taxonomy/` is injected with an EXPLICIT `@Inject(token)`, including the class-typed ones, while the rest of `apps/api` relies on type-inferred constructor injection. Cause: the root-level `endpoint-authz` gate boots the whole AppModule graph under `tsx`, whose esbuild transform does not implement `emitDecoratorMetadata`, so `design:paramtypes` is absent and Nest resolves a type-inferred param to `undefined`. Today that is INVISIBLE elsewhere only by luck — a class-typed param with no `@Inject` sibling makes Nest see zero declared deps and inject nothing, so `EventsService.repo` is silently `undefined` in that boot (harmless there: the gate only enumerates routes and never calls a service). A route class that mixes a token `@Inject` at a LATER index with earlier class-typed params fails loudly instead, which is how this surfaced. `nest build` (tsc) emits the metadata, so production DI is unaffected. Below the §6 threshold — a documented local convention (module README) with no runtime effect on the shipped artifact (promote-when: the gate is changed to boot through a metadata-emitting transform (e.g. run it against `dist/` or via ts-node/swc with `emitDecoratorMetadata`), which would let the whole api drop the explicit tokens; or a second module hits the same trap — then fix the gate rather than spreading the convention)
- [ ] 2026-08-20 Issue #1304 — nine admin mutations are EXEMPT from live IdP authority revalidation and are recorded as such in `REVALIDATION_EXEMPT` (`apps/api/src/authz/admin-revalidation-coverage.spec.ts`): feature 007's `POST /v1/admin/events`, `PATCH /v1/admin/events/:id`, `POST /v1/admin/events/:id/{open,close,archive,publish,transition}`, `PUT /v1/admin/events/:id/stream`, and feature 011's `DELETE /v1/admin/users/:id/mfa`. Not an oversight and not a gap in the "no handler can bypass" AC — the coverage spec is default-deny, so each of these is a written decision a reviewer reads, and every route added from now on fails the spec until it is wired or consciously excused. The reason they are excused HERE is blast radius: both features are live in production under their own authority model, and adding a synchronous IdP round-trip (plus a new 503 failure mode) to a shipped surface is a deliberate change to that surface's availability contract, not a side effect of a 012 hardening Issue — 007's transitions in particular are operated during a live эфир, where a Zitadel blip turning `close` into a 503 is a worse outcome than the stale-authority window it closes. The auth-tier entry routes and `POST /v1/admin/auth/logout` in the same registry are PERMANENT exemptions by construction (no session to revalidate; refusing a logout during an outage strands the operator) and are NOT part of this debt. Below the §6 Issue threshold: no product deliverable is blocked, nothing is user-visible, and the security posture is unchanged from what 007/011 ship today (promote-when: the ADR-0009 erasure-plan approval route lands under #1306 and gives `revalidate: "live"` a second production consumer with observed 503 rates — then re-decide 007 and 011 against real availability data, wiring them in one PR that also gives the admin UI a retry path for `IDP_REVALIDATION_UNAVAILABLE`; or a stale-authority incident is observed on either surface, which promotes this to an Issue immediately)
- [ ] 2026-08-20 PR #1381 (#1339) — the admin error dictionary maps `IDP_REVALIDATION_UNAVAILABLE` to a human RU sentence only in the RECORDINGS scope (`apps/admin/lib/taxonomy-errors.ts` → `recordings.errors.authorityUnavailable`, added with the «Записи» tab). The taxonomy scopes wired to `revalidate: "live"` since #1409/#1414 — projects, experts and now topics admin mutations — have no mapping for that code, so an operator hitting a Zitadel outage there gets the generic fallback sentence instead of the actionable «повторите позже» copy the recordings tab shows. Purely operator-facing copy during an IdP outage; the refusal itself is correct and server-enforced, so below the §6 Issue threshold (promote-when: the #1304 DEBT entry's re-decision PR lands — it already plans an admin retry path for `IDP_REVALIDATION_UNAVAILABLE`, and the mapping should ride that PR for every `live` scope at once; or an operator reports the confusing generic error in the wild)
- [ ] 2026-08-21 PR #1421 (#1419) — the 10GB build-cache cap lives in THREE places with no machine-enforced parity: `BUILD_CACHE_RESERVED_SPACE` in `tools/deploy/prod.mjs` (deploy-step prune), the `defaultReservedSpace` value embedded in `infra/deploy/cloud-init/api-prod.yaml` daemon.json, and the README runbook prose. Cross-reference comments name the change-all-three rule, but a YAML cannot import a JS constant and no lint hook parses cloud-init values, so a drift fails silently — the looser figure wins and the disk refills to it. Below the §6 threshold: no product deliverable blocked, not user-visible, worst case is a fatter-than-intended cache on a box that now has two independent GC layers (promote-when: the cap value is ever changed, or a second synced literal appears between `tools/deploy/` and `infra/deploy/` — then add a small `tools/lint/` guard that parses the cloud-init daemon.json and asserts it equals the prod.mjs constant)
- [ ] 2026-08-21 PR #1420 (#1278) — `registrations` and `users` READS are still unfiltered on the new `record_status` lifecycle axis (`apps/api/src/registrations/*.repository.ts`, `apps/api/src/users/*` query paths). Inert today: no production path can retire either row kind yet — the #1420 write-path work only added the columns and the RESTRICT FKs, and every existing read sees exclusively `active` rows because nothing else exists. The active-projection filter therefore has no observable effect until a retire path ships. Below the §6 Issue threshold — no product deliverable blocked, no user-visible behavior, no prod risk while the axis is write-only (promote-when: any feature adds a retire/soft-delete transition for users or registrations — that PR MUST add the `record_status = 'active'` default-read filter to both repositories in the same change, per the ADR-0003 design §3.6 rule the other repositories already follow)
- [ ] 2026-08-22 PR #1438 (#1434) — `apps/docs/content/specs/product/two-site-ia/requirements-ru.md` still describes OWD-12 as an open fork in REQ-22 (l.45) and REQ-113 (l.161) («DS хранит документы vs получает статус от лицензиата»), while `one-way-doors-ru.md` §OWD-12 and `next-steps-ru.md` record it accepted at DSP-252, variant (a) — DS stores copies of the doctor's documents. The file is the verbatim REQ registry moved from `bbm`: its numbering is the annotation key of `wireframe.html`, so editing REQ bodies in place would desynchronise the mock-up from the register for a wording-only gain — below the AGENTS.md §6 threshold (no product deliverable blocked, nothing user-visible, no prod or data risk; the authoritative OWD status lives in `one-way-doors-ru.md`, which is correct) (promote-when: stage 3 lifts REQ-22/REQ-113 into feature PRDs and the registry is retired as source material, or a reader acts on the stale wording and the drift causes a real design/spec error)
- [ ] 2026-08-24 /wrap retro (session 8b8dce2f) — the context-budget hook is advisory at 200K/250K for the LEAD but never names the AGENTS.md §6 300K retro line or a remedy, so a lead ingesting bulk tool payloads (the 2026-08-24 vendoring session peaked at 540K on 32 DesignSync `get_file` returns) gets no actionable signal at the threshold. Extend the hook: at 300K print the §6 retro-finding line plus the standing remedy — dump bulk payloads to files (`tool-results/` extraction per memory `reference_designsync_vendoring_recipe`) and split the remaining work across sessions. Below the §6 Issue threshold — advisory tooling, no product deliverable blocked, no prod risk (promote-when: a second lead session crosses 300K after this line lands — then the advisory demonstrably does not steer and the hook work becomes an Issue with `source:retro`)
- [ ] 2026-08-24 PR #1462 (#1453) — subagent dispatch cwd inheritance is NOT repo-side-fixable: a dispatched agent's starting cwd is assigned by the Claude Code harness (arrives as PreToolUse `cwd`); `.claude/settings.json` exposes no subagent-cwd setting and a PreToolUse hook can only allow/warn/block, never relocate a session. The repo-side mitigation shipped in #1462: a stale-cwd dispatch now trips a deterministic `wrong-worktree` refusal on its first write, prescribing the exact `EnterWorktree path:<target>` recovery — so the failure is self-describing instead of mislabeled as `main-tree escape`. Below the §6 Issue threshold — the recurring blocker is defused by the message fix; residual is one extra `EnterWorktree` per affected dispatch (promote-when: the harness grows a dispatch-cwd control (report upstream then), or the wrong-worktree recovery itself fails a dispatched agent in the wild)
- [ ] 2026-08-24 /wrap retro (session 1848849b) — `pnpm pr:land <N>` does not tear down issue N's worktree itself: the lead must remember `worktree:teardown <N>` FIRST or the gate refuses (exit 4) and the command is re-run (fired once this session on #1459; correct on #1462/#1463 only by memory). Now that #1463 gave teardown a safe dirty-tree refusal, fold the teardown INTO `pr:land` (after the dirty-check, before the gate) so the ordering cannot be got wrong and the memory bullet becomes a pointer. Below the §6 Issue threshold — cost of the miss is one re-run, nothing lost (promote-when: the ordering is missed twice in one session after this line lands, or the dirty-refusal path misfires inside pr:land)
- [ ] 2026-08-24 /wrap retro (session 1848849b) — Bash tool cwd resets between calls, so a `cd <worktree> && cat >> <file>` compound followed by a next-call relative-path operation silently targets whatever tree the shell now sits in; this session burned three probe calls re-establishing WHICH tree a DEBT.md append landed in. Extend the #1462 worktree-path-guard with a WARN on a Bash command combining a `cd <worktree>` prefix with a relative-path write (`>>`, `cat >`, `tee`), recommending `git -C <abs>` / absolute paths. Below the §6 Issue threshold — advisory-only, no data lost (promote-when: a relative-path write actually lands in the WRONG tree, i.e. the trap causes a misplaced edit rather than extra probes)
- [x] 2026-08-26 PR #1560 (#1478) — the `registry-research` guard user-facing-UI app list omits `apps/doctor` — **promoted to Issue #1564** (retro 2026-08-26: a gap in a CI guard gating other PRs is above the §6 threshold by definition; AGENTS.md clause added same day). Fix alongside #1444 with the dead-glob self-test.
- [ ] 2026-08-26 /wrap retro (session 882b12a3) — the #700-M1 dispatch-guard hook counts lead mutations in the MAIN tree only, so a lead doing full inline implementation inside `.claude/worktrees/**` (PR #1563: worktree, extraction script, design-source writes, README row, commit, PR, land — zero dispatches) draws no WARN at all. Extend the hook WARN counter to lead writes under `.claude/worktrees/**` (same ≤2 consecutive-mutation cap; subagent writes exempt — they are identified by the dispatch env). Below the §6 Issue threshold — advisory guard, no product deliverable blocked, gates no other PR (promote-when: a second fully-inline worktree PR lands after this line, i.e. the blind spot demonstrably keeps recurring)
- [ ] 2026-08-26 Issue #1479 — the two-site-IA discovery package still carries «105»-literal specialty-count claims that the closed Минздрав book (ADR-0016 / 017) contradicts: `apps/docs/content/specs/product/two-site-ia/requirements-ru.md:145`, `discovery-glossary-ru.md:53`, `design-prompts-ru/01-d-home-ru.md:33,60,82,83,95`, `wireframe.html:111,112`. Not a product-blocking gap — the authoritative count is the seeded book itself and no shipped surface reads these numbers — so below the §6 Issue threshold (promote-when: the discovery package is next edited for any reason, then sweep every «105» literal in the same PR rather than one file at a time)
- [ ] 2026-08-26 Issue #1479 — the specialty-book RE-SEED obligation has no named holder: a new or amended Минздрав order changes the closed book, and neither ADR-0016 nor `017-design` says who notices, who re-runs the seed, or how a removed code is reconciled with rows that already reference it. The boot-time seed is idempotent and additive, so nothing breaks silently today; the gap is procedural, not runtime (promote-when: an ops runbook for reference books exists to hold the obligation, or an actual order amendment lands and the reconciliation has to be improvised)
- [ ] 2026-08-26 Issue #1479 — `apps/api/src/storefront/specialties.errors.ts` imports `PROBLEM_TYPE_BASE` and `resolveTraceId` from `../taxonomy/taxonomy.errors.js`; both are transport-level RFC-9457 plumbing with nothing taxonomy-specific about them, so the storefront module now depends on taxonomy purely for envelope construction. Two consumers is not yet an abstraction (promote-when: a THIRD module needs them — then lift both into a shared problem module and re-point taxonomy and storefront at it in the same PR)
- [ ] 2026-08-26 Issue #1479 — the `specialties_minzdrav.frequent_rank` column extends the 017-design §2 ER model, which lists only `{uuid id, text code, text name, bool is_other}`; the column is needed by the EARS-3 `frequent` read and is shipped, so code and spec disagree on the entity shape. Documentation drift only — the migration, schema SSOT and tests are consistent (promote-when: `017-design.md` is next edited, then sync §2 to the shipped columns in that PR)
- [ ] 2026-08-27 /wrap retro (session a29b02a8) — a task-notification from a PREVIOUS session's still-live agent is indistinguishable from the current dispatch's return (only the session id inside its output-file path differs), which caused a duplicate implementer dispatch onto the same #1482 worktree; the collision produced no broken artifact by luck. Remedy: a PreToolUse guard on `Agent` that warns when the brief names a worktree already claimed by a LIVE agent (ListAgents cross-check), plus stale-notification marking by session-id mismatch. Below the §6 threshold — advisory orchestration guard (promote-when: a second cross-session duplicate dispatch occurs)
- [ ] 2026-08-27 /wrap retro (session a29b02a8) — the 150K subagent ROTATE line is self-waivable in form: an implementer finished at 167K returning plain `DONE (no rotation needed)`, indistinguishable from an under-budget DONE. Remedy: the context-budget hook requires the return's first line to carry either `ROTATE:` or `OVER-150K-DONE: <reason>` once the threshold fired (promote-when: an over-150K silent continuation actually loses a checkpoint or corrupts a return)
- [ ] 2026-08-27 /wrap retro (session a29b02a8) — api boot on a Stage-B stand failed on `LIFECYCLE_IMPACT_TOKEN_SECRET` missing from `~/.ds-platform/.env.local` (fixed by writing the key into the recipe file, not an inline export). Remedy: a `dev:env:check` preflight that diffs the api env schema's required keys against `.env.local` and prints missing keys + dev defaults before any stand boot (promote-when: a missing-env boot failure recurs despite the recipe fix)
- [ ] 2026-08-27 PR #1582 (#1578) — the `showcase-snippet` guard (`tools/lint/showcase-snippet-lint.ts`) flags a GENERIC TYPE NAME inside a string literal as a hand-typed usage snippet: its `JSX_IN_LITERAL_RE` (`/<[A-Z][A-Za-z0-9]*[\s/>]/`) matches `DataTableColumn<Row>[]`, `DataTableRecordColumn<Row>` and `Omit<EmptyStateProps, 'variant'>` in the `type` field of a showcase prop-table row — a type SIGNATURE, not depicted component usage. The only escape hatch is the file-wide `showcase-snippet-ok:` suppression, which would blind the whole 1500-line `apps/showcase/app/blocks/blocks-view.tsx`, so #1582 worked around it by rewriting those three `type` strings in prose («DataTableColumn of the row type — array»), losing signature fidelity in the catalogue. Fix = teach the guard to ignore generic type arguments inside literals (require the match to look like a JSX tag — e.g. reject when the `<…>` is preceded by an identifier char, or when the literal parses as a lone type expression). Below the §6 Issue threshold — a guard-heuristic false positive with a recorded prose workaround, no product deliverable blocked, nothing user-visible, and the guard gates no other PR incorrectly today (promote-when: a SECOND showcase section has to distort its prop-table types to pass, or an author reaches for the file-wide suppression — then the blind spot is actively degrading the catalogue and the guard fix becomes an Issue)
- [ ] 2026-08-27 PR #1582 (#1578) — the showcase's fixed theme-toggle button overlaps page content at 390px (it sits on top of the first `DataTable` record card and covers part of its «Код номенклатуры» value). Pre-existing chrome, NOT introduced here: `apps/showcase/app/_components/theme-toggle.tsx` and `app/layout.tsx` are untouched by this PR's diff (`git diff origin/main -- apps/showcase` = `blocks-view.tsx` + `registry.ts` only), so it reproduces on `origin/main` for every long section at that width. Fix = give the toggle a mobile placement that does not overlay content (a sticky header slot, or bottom-anchored with a matching page inset). Below the §6 Issue threshold — internal catalogue chrome, no product surface and no user affected (promote-when: the showcase is handed to the owner as the Stage-B surface at mobile width and the overlap actually obstructs a verdict)
- [ ] 2026-08-27 PR #1582 (#1578) — `DataTable` clickable rows still have no press state distinct from hover, which ADR-0013 §7 lists in the mandatory per-element set (default / hover / active / focus-visible / disabled / loading). Pagination numbers got the press this round (the `FilterChip` neo-brutalist nudge, `pagination.tsx`), but the same nudge on a full-width `<tr>` would shift the whole grid, and the token set has no darker step of `tint` to press into (`--color-tint` is a single value; `accent` is a neutral grey, wrong direction on a blue-tinted row). A correct fix is a new `tint-pressed` token, i.e. a design-token addition under ADR-0013 §3 — an owner Stage-A call, not a lead call inside this PR. Below the §6 Issue threshold — the admin shell that consumes `DataTable` is not shipped yet, so no operator is affected today (promote-when: the first real admin list surface goes to Stage-B, which is when a missing row press becomes a live defect rather than a catalogue gap)
- [ ] 2026-08-27 PR #1584 (#1583) — `tools/backlog-triage.ts` `missingFields()` does not check `track:*`, so the «pre-existing Issues are backfilled on touch» rule that PR #1584 wrote into `.claude/rules/repo-conventions.md` has no detector: `pnpm backlog:triage`'s `## Field hygiene` section lists Issues missing a kind label, a `source:*` or a milestone, but a track-less Issue looks clean there. Creation is already fail-closed (`tools/gh/create-issue.mjs` gate), so only the pre-gate backlog is affected and no new Issue can slip through. Below the §6 Issue threshold — a reporting gap in an advisory sweep, not a CI guard gating PRs, no product deliverable blocked (promote-when: `tools/backlog-triage.ts` is next touched for any reason — add `track:*` to the field-hygiene sweep in that PR; or the manual backfill stalls and the missing Issues need a machine listing)
- [ ] 2026-08-27 PR #1582 (#1578) — the `Badge` `label` variant fills with `bg-tint` (`packages/design-system/src/primitives/badge.tsx:29`), the exact fill a `DataTable` row takes on hover (`packages/design-system/src/blocks/data-table.tsx:244,309` — `hover:bg-tint`), so a bare `<Badge>` used as a status chip inside a row dissolves into the row on hover (rgb(211,232,253) on both). Fixed here at the SHOWCASE CONSUMER, not in the package: the demo's chips now carry the semantic tones `constitution.md` → DataTable "Token / primitive mapping" prescribes (`bg-success-tint text-success-text` / `bg-warning-tint text-foreground`), which never collide with the row tint. The package-internal seam stands: the primitive still offers a chip fill that is invisible on a hovered row, and the `label` variant's `bg-tint` is pinned by a class-contract test (`badge.test.tsx:41`), so changing it is a design-token/variant decision, not a consumer fix. Every future `DataTable` consumer must therefore style status chips semantically per `constitution.md:223`. Recorded second: the token set has `success-text` and `destructive-text` but NO `warning-text`, so the «Черновик» chip pairs `warning-tint` with the theme-flipping `foreground` (`warning-foreground` is near-black in BOTH themes and unreadable on the dark `warning-tint` #332612). Below the §6 Issue threshold — the admin shell that consumes `DataTable` is not shipped, no operator is affected today, and the catalogue surface is correct (promote-when: a consumer ships a bare `bg-tint` `Badge` inside a `DataTable` row — then fix at the primitive/variant level, adding the missing `warning-text` token in the same pass)
- [ ] 2026-08-27 /wrap retro (session f7c78f23) — hand-typed licence notices are un-guarded: a wrong «Copyright (c) 2024 Hayden Bleasel» Kibo notice passed 28/28 static + 34/34 live gates and one full Mode-a round (PR #1582, caught only at re-review). Remedy: an adoption manifest per vendored block ({registry, upstream repo, LICENSE URL, fetch date, notice hash}) + a static check comparing the in-file header notice against the manifest byte-for-byte (promote-when: the next block-adoption PR runs, or a second wrong notice is caught — then the guard rides that PR)
- [ ] 2026-08-27 /wrap retro (session f7c78f23) — Stage-B GO recording is two-place and order-fragile: the GO landed as an Issue comment but the PR body still said «Stage-B: pending», so `pr:preflight --pre-merge` failed and the body needed a manual sed 17 minutes later. Remedy: `pnpm stageb:go <pr> "<verbatim>"` writing BOTH the linked-Issue comment and the PR-body line in one call (promote-when: the stale-marker trap fires a second time)
- [ ] 2026-08-27 /wrap retro (session f7c78f23) — a lead build command (`pnpm --filter @ds/showcase build`) ran in the MAIN tree instead of the session worktree once (self-caught, TaskStop'd, tree verified clean). Remedy: extend the worktree-path-guard PreToolUse WARN to build/test/lint Bash commands that resolve to the main tree while `.claude/worktrees/<active-issue>` exists (promote-when: a main-tree build actually dirties the shared tree rather than costing a kill)
- [ ] 2026-08-27 /wrap retro (session f7c78f23) — a known-issue code comment is invisible to `surface-decision-debt`: the Badge×row-hover collision was first recorded ONLY as a comment at the fixed consumer (Mode-a round-4 blocker; DEBT line added on rework). Remedy: a static check — comments in `packages/design-system/` + `apps/showcase/` matching known-issue markers (collision / workaround / known issue / for now) must carry a `DEBT.md` anchor or `#<issue>` ref (promote-when: a second untracked-seam comment survives a review round)
- [ ] 2026-08-27 PR #1597 (#1338) — the 014 EARS-18 route `POST /v1/admin/events/:id/mark-ended` answers its PROTOCOL refusals (428 `IDEMPOTENCY_KEY_REQUIRED`, 400 `IDEMPOTENCY_KEY_INVALID`, 409 `IDEMPOTENCY_KEY_REUSED`) in feature 007's `{ code, message }` envelope instead of the RFC 7807 Problem Details body with `traceId` that `014-design` §11 prescribes for every 014 failure (`apps/api/src/events/events.admin.controller.ts` → `withProtocolRefusalShape`). The error CODES are exactly §11's; only the envelope and the `traceId` field deviate. Deliberate: the route extends the shipped 007 admin controller (014-design §3.1 Protocol row — «exactly like every other 007 transition»; route table :355), whose four live sibling commands (`publish`/`open`/`close`/`archive`) all answer `{ code, message }`, and the admin data-provider parses that shape — dragging 012's `TaxonomyProblemFilter` onto the controller would reshape four routes already live in production for the sake of one new one. Below the §6 Issue threshold: no product deliverable blocked, the operator-visible behaviour is correct and tested, and no prod risk — the same class of envelope debt as the `If-Match` foundation tracked at #1593 (promote-when: the 007 admin controller migrates to RFC 7807 wholesale — then mark-ended rides that PR and `traceId` arrives for all five commands at once; or a second 014 route has to land on this controller, i.e. the envelope split stops being one route wide — alternatively, if the split is accepted permanently, carve it out inline in `014-design` §11)
