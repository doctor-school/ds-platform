---
title: Wave-1 entry gate — auth-flow behaviour matrix, EARS disposition, PR sequence
status: Accepted 2026-09-15 — every row carries an explained difference and a test id or the PR that owes it; wave 1 (#2027) may open its code PRs
---

# Wave-1 entry gate — `@ds/auth-flow`

## 1. Purpose and established facts

This is the wave-entry gate required by `2026-09-07-one-code-two-storefronts-plan-en.md` §4 («Wave-entry gate», the paragraph opening «The wave Issue carries a **behaviour matrix**»). No PR of wave 1 opens until every row below carries a test id and an explained difference.

- Scope: the auth surfaces of both storefront hosts — Academy (`apps/portal`) and doctor (`apps/doctor`) — that wave 1 folds into `packages/auth-flow`. That package does not exist yet; this document is slice 0 of #2027 and creates no code.
- `grep -rn "mirror-of:" apps/doctor` returns **0 matches** today. Step (d) of the four wave steps (§4, «(d) `mirror-of` markers for that capability are gone») is therefore satisfied by construction for wave 1 and must not be re-invented as work.
- Shell hand-off: #2180 already moved `components/header-user-cluster.tsx` and the theme unit into `packages/storefront-shell`. The shell exposes the auth cluster as a host-config slot; **wave 1 fills that slot**, it does not own theme or header chrome. The §4 wave-1 table still lists `header-user-cluster` and `theme` under «Moves from `apps/portal`» — superseded by #2180, and PR 1.2 of the §4 sequence is already delivered.
- Already shared before wave 1: `packages/design-system/src/blocks/{smart-captcha,bot-protection-field,use-bot-protected-action,bot-protection-error}.tsx` (registry row «Bot protection (client half)», `capability-ownership.md:44`), `blocks/pending-registration.ts` (held-password slot, row 42), `blocks/registration-success-card.tsx` (row 43), the auth cards / shell blocks (row 41). Wave 1 owns the **compositions**; design-system keeps the **cards** (§3 rule 1).
- Design entry: `design-source/auth.dc.html`, the ONE host-switched auth canvas (props `host`, `captcha`, `error`), vendored 6cb9930f — the §4 canvas precondition (#2080 → #2027) is met. Owner, 2026-09-15 on #2027: the captcha plaque in that canvas is a **placeholder** for the Yandex SmartCaptcha widget; the package mounts the existing `smart-captcha` / `bot-protection-field` blocks. The canvas fixes only the slot geometry and the four states «не пройдена» (submit disabled) / «пройдена» / «истекла» / «сбой» — our copy and submit-blocking rules. ui-parity excludes the widget box itself.

## 2. Behaviour matrix

One row per scenario. `Allowed difference` takes exactly one of: `none` · `config: <field>` · `product divergence: <ref>` · `convergence: <owner quote/date>` · `OPEN — owner Stage-A`. Every `OPEN` row and every row without a test id is repeated in section 5.

Anchors are `file:line` in this repository at `553ef3be`. A cell that could not be anchored reads `UNVERIFIED — <why>`, never a guess.

### 2.1 PR 1.1 — `room-return` codec into `@ds/room`

| #   | Scenario                                                                                              | Academy behaviour (file:line)                                | Doctor behaviour (file:line)                                                                      | Shared contract                                                           | Allowed difference                                                        | Test id(s)                                                                         |
| --- | ----------------------------------------------------------------------------------------------------- | ------------------------------------------------------------ | ------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------- | ------------------------------------------------------------------------- | ---------------------------------------------------------------------------------- |
| 1   | A canonical `/webinars/<slug>/room` target is parsed and the canonical room path reconstructed        | `apps/portal/lib/room-return.ts:47` `parseRoomReturnTarget`  | absent (flag off) — this host serves no room route; its room read is `apps/doctor/lib/room.ts:33` | ONE room-return codec in `@ds/room`; the host supplies the route template | `config: routes.room` (path template; `undefined` = no room on this host) | `apps/portal/lib/room-return.test.ts:21`                                           |
| 2   | A bare event page (no `/room` suffix) is NOT a room return                                            | `apps/portal/lib/room-return.ts:47`                          | absent (flag off)                                                                                 | same codec                                                                | `config: routes.room`                                                     | `apps/portal/lib/room-return.test.ts:29`                                           |
| 3   | A cross-origin / open-redirect / traversal room target, and a non-string, are rejected                | `apps/portal/lib/room-return.ts:67` `isSafeRoomReturnTarget` | the same guard, reached through `apps/doctor/lib/return-context.ts:133` `resolveReturnTargetPath` | one same-origin guard — `parseReturnTarget` in `@ds/schemas`              | none                                                                      | `apps/portal/lib/room-return.test.ts:33`, `apps/portal/lib/room-return.test.ts:70` |
| 4   | A doctor-feed-shaped target is not a room return on the Academy host                                  | `apps/portal/lib/room-return.ts:47`                          | n/a — the feed shape is this host's own (`apps/doctor/lib/return-context.ts:120`)                 | one codec, host-scoped route table                                        | `config: routes.room`                                                     | `apps/portal/lib/room-return.test.ts:50`                                           |
| 5   | A room href is built from a slug, escaping a hostile slug so it can never front a cross-origin target | `apps/portal/lib/room-return.ts:78` `buildRoomReturnHref`    | absent (flag off)                                                                                 | same builder                                                              | `config: routes.room`                                                     | `apps/portal/lib/room-return.test.ts:76`                                           |

### 2.2 PR 1.3 — auth client, the one error dictionary, bot protection, field rules

| #   | Scenario                                                                                                                                                                                | Academy behaviour (file:line)                                                                              | Doctor behaviour (file:line)                                                                                                         | Shared contract                                                                                                           | Allowed difference                                                                                                                                                                                                                                                                                                                                             | Test id(s)                                                                                                                                                                                                                                         |
| --- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------ | ------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 6   | The auth client POSTs the relative BFF path so the `__Host-` session cookie rides along                                                                                                 | `apps/portal/lib/auth-client.ts:100` `authClient`                                                          | `apps/doctor/lib/auth-client.ts:120`                                                                                                 | ONE `authClient` in `@ds/auth-flow/client`; both hosts already rewrite `/v1/:path*` to the api                            | none                                                                                                                                                                                                                                                                                                                                                           | `apps/doctor/lib/auth-client.test.ts:35`                                                                                                                                                                                                           |
| 7   | The sign-in-code request and its verification hit their own relative endpoints                                                                                                          | `apps/portal/lib/auth-client.ts:100`                                                                       | `apps/doctor/lib/auth-client.ts:136`, `:141`                                                                                         | same client, one endpoint table                                                                                           | none                                                                                                                                                                                                                                                                                                                                                           | `apps/doctor/lib/auth-client.test.ts:50`                                                                                                                                                                                                           |
| 8   | A non-2xx becomes an `AuthError` carrying the status and the api code; a non-JSON failure body still yields one, never a parse crash                                                    | `apps/portal/lib/auth-client.ts:100` — a stable challenge code is preserved without parsing exception copy | `apps/doctor/lib/auth-client.ts:159`                                                                                                 | one error envelope; callers read codes, never copy                                                                        | none                                                                                                                                                                                                                                                                                                                                                           | `apps/portal/lib/auth-client.test.ts:8`, `apps/doctor/lib/auth-client.test.ts:71`, `apps/doctor/lib/auth-client.test.ts:83`                                                                                                                        |
| 9   | Field rules: the password error restates the single length rule, the promo code is optional/trimmed/bounded, the confirmation code accepts a lowercase alphanumeric value untransformed | `apps/portal/lib/identifier-validation.ts:138`; the promo field does not exist on this host                | `apps/doctor/lib/register-fields.ts:72`, `:90`, `:102`                                                                               | ONE rule set in the package; the host switches the promo field on                                                         | `config: register.promoField`                                                                                                                                                                                                                                                                                                                                  | `apps/doctor/lib/register-fields.test.ts:32`, `:47`, `:60`; `apps/doctor/e2e/register-validation.spec.ts:90`, `:121`, `:162`; `apps/portal/e2e/identifier-validation.e2e.spec.ts:253`, `:270`                                                      |
| 10  | A 401 and a 400 stay the caller-neutral generic — no existence oracle                                                                                                                   | `apps/portal/lib/auth-error-message.ts:31` `authErrorMessage`                                              | `apps/doctor/lib/auth-error-message.ts:49` `AUTH_GENERIC_MESSAGES`                                                                   | ONE dictionary keyed by status + api code                                                                                 | `config: copy`                                                                                                                                                                                                                                                                                                                                                 | `apps/doctor/lib/auth-error-message.test.ts:19`                                                                                                                                                                                                    |
| 11  | The generic sentence is the CALLER's own — a verification failure never says «войти»                                                                                                    | `apps/portal/lib/auth-error-message.ts:31`                                                                 | `apps/doctor/lib/auth-error-message.ts:64`                                                                                           | one dictionary, per-caller generic slot                                                                                   | `config: copy`                                                                                                                                                                                                                                                                                                                                                 | `apps/doctor/lib/auth-error-message.test.ts:30`                                                                                                                                                                                                    |
| 12  | A 429 is a rate limit with its own actionable RU copy, never a wrong-credential sentence                                                                                                | `apps/portal/lib/auth-error-message.ts:37` (already mapped)                                                | `apps/doctor/lib/auth-error-message.ts:49`                                                                                           | same dictionary entry                                                                                                     | `config: copy`                                                                                                                                                                                                                                                                                                                                                 | `apps/doctor/lib/auth-error-message.test.ts:39`                                                                                                                                                                                                    |
| 13  | The **confirmation-step** submit maps through that same dictionary (#2001)                                                                                                              | `apps/portal/app/verify/page.tsx:244` `onInvalid` routes through `authErrorMessage`                        | `apps/doctor/components/registration-screen.tsx:368` raises without the dictionary — the 429 on the confirm submit is unmapped today | one dictionary for every auth submit, confirmation included                                                               | `convergence: owner chat 2026-09-07 — «лимит попыток должен звучать иначе, чем неверный код»`                                                                                                                                                                                                                                                                  | owed: a doctor confirm-step 429 branch test in PR 1.3 (`apps/doctor/lib/auth-error-message.test.ts`); resend half already covered by `apps/doctor/lib/storefront-auth-client.test.ts:108`                                                          |
| 14  | A 5xx and a rejected fetch both read as availability, never as a wrong password                                                                                                         | `apps/portal/lib/auth-error-message.ts:31`                                                                 | `apps/doctor/lib/auth-error-message.ts:49`                                                                                           | same dictionary entry                                                                                                     | `config: copy`                                                                                                                                                                                                                                                                                                                                                 | `apps/doctor/lib/auth-error-message.test.ts:48`                                                                                                                                                                                                    |
| 15  | A bot-protection refusal names the real obstacle (#1558), not the credential                                                                                                            | `apps/portal/lib/bot-protection.ts:24` `botProtectionMessages`                                             | `apps/doctor/lib/auth-error-message.ts:49`, `apps/doctor/lib/bot-protection.ts:36`                                                   | the machine-readable guard code, mapped once                                                                              | `config: copy`                                                                                                                                                                                                                                                                                                                                                 | `apps/doctor/lib/auth-error-message.test.ts:61`, `apps/doctor/lib/auth-client.test.ts:138`                                                                                                                                                         |
| 16  | **The submit while a mandatory condition is unmet** (unsolved captcha «не пройдена» among them)                                                                                         | submits stay ENABLED; the reason is stated on the attempted submit — `apps/portal/app/verify/page.tsx:244` | the submit is INERT with its reason stated beside it and wired to the control — `apps/doctor/app/(auth)/register/page.tsx:90`        | one submit-gating rule for every auth surface of both hosts                                                               | `convergence: owner decision 2026-09-15 — «Больше склоняюсь к тому, как это в Академии»` — the submit stays ENABLED on both hosts; the attempted submit reveals every field error, the unsolved captcha among them, and moves focus to the first one. #1548's «disabled submit that always states its reason» clause is superseded; #1952 is not adopted here. | `apps/portal/app/verify/page.test.tsx:446`; owed: `apps/doctor/e2e/register-layout.spec.ts:204`, `apps/doctor/e2e/register-medworker.spec.ts:94`, `apps/doctor/e2e/register-mobile.spec.ts:436` are REWRITTEN to the enabled-submit rule in PR 1.6 |
| 17  | The challenge is invisible and minted per submit: a submit executes a fresh challenge and resumes the command exactly once with its token                                               | `apps/portal/app/register/page.tsx:124`, `apps/portal/app/login/page.tsx:199`                              | `apps/doctor/lib/storefront-auth-client.ts:102`, `apps/doctor/lib/bot-protection.ts:24`                                              | `use-bot-protected-action` (design-system row 44) stays the client half; the package owns the auth compositions around it | none                                                                                                                                                                                                                                                                                                                                                           | `apps/portal/app/register/page.test.tsx:115`, `apps/portal/app/login/page.test.tsx:143`, `apps/doctor/lib/storefront-auth-client.test.ts:76`, `apps/doctor/e2e/register-bot-protection.spec.ts:66`                                                 |
| 18  | With no minted token the `x-smartcaptcha-token` header is ABSENT (matching the api guard), and an unconfigured site key renders no processing notice at all                             | `apps/portal/lib/bot-protection.ts:19` `botProtectionSiteKey`                                              | `apps/doctor/lib/storefront-auth-client.ts:102`                                                                                      | one header contract; the host names the site-key env only                                                                 | `config: botProtection.siteKeyEnv`                                                                                                                                                                                                                                                                                                                             | `apps/doctor/lib/storefront-auth-client.test.ts:96`, `apps/portal/components/auth-shell.test.tsx:148`                                                                                                                                              |
| 19  | A refused or expired token («сбой» / «истекла») gets truthful CAPTCHA feedback and a fresh retry without losing form data                                                               | `apps/portal/app/register/page.tsx:124`                                                                    | `apps/doctor/lib/storefront-auth-client.ts:102`                                                                                      | one refusal code, one retry rule; the canvas fixes the four states (§1)                                                   | `config: copy`                                                                                                                                                                                                                                                                                                                                                 | `apps/portal/app/register/page.test.tsx:137`, `apps/doctor/lib/storefront-auth-client.test.ts:185`, `:199`                                                                                                                                         |
| 20  | Every code resend carries the token too (the resend route is `@BotProtected`), while the confirmation submit itself stays challenge-free                                                | `apps/portal/app/verify/page.test.tsx:149`, `:159`; `apps/portal/app/login/page.tsx:213`                   | `apps/doctor/lib/storefront-auth-client.ts:123`, `:114`                                                                              | one protected-route list in the package                                                                                   | none                                                                                                                                                                                                                                                                                                                                                           | `apps/portal/app/verify/page.test.tsx:149`, `:159`, `apps/doctor/lib/storefront-auth-client.test.ts:108`, `:122`                                                                                                                                   |
| 21  | The identifier accepts an email or an E.164 phone, rejects a bare-numeric value, and masks free typing to E.164 on the SMS channel                                                      | `apps/portal/lib/identifier-validation.ts:55`, `:70`, `:82`, `:113`, `:161`                                | `apps/doctor/lib/register-fields.ts:33` — the email rule only; this host serves no phone channel                                     | ONE identifier validator; the host declares which channels it serves                                                      | `config: channels` (OTP channel enum list; `['email']` on the doctor host)                                                                                                                                                                                                                                                                                     | `apps/portal/e2e/identifier-validation.e2e.spec.ts:91`, `:115`, `:126`, `:142`, `:175`; `apps/doctor/lib/register-fields.test.ts:22`, `apps/doctor/e2e/register-validation.spec.ts:64`                                                             |

### 2.3 PR 1.4 — session read, header auth cluster, middleware parking, return-target codec

| #   | Scenario                                                                                                                               | Academy behaviour (file:line)                                                                                                                                                                                                      | Doctor behaviour (file:line)                                                                                                                           | Shared contract                                                                                              | Allowed difference                                                                                                                                                                                                                                                                                                                                                                              | Test id(s)                                                                                                                                                                                                 |
| --- | -------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------ | ------------------------------------------------------------------------------------------------------------ | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 22  | The session cookie is the `__Host-` origin-locked name (ADR-0015 §4), recognised only on a NAME boundary                               | `apps/portal/lib/header-auth.ts:38` `HeaderAuth`                                                                                                                                                                                   | `apps/doctor/lib/session.ts:49`                                                                                                                        | ONE cookie name constant in `@ds/auth-flow/server`                                                           | none                                                                                                                                                                                                                                                                                                                                                                                            | `apps/doctor/lib/session.test.ts:21`, `:28`                                                                                                                                                                |
| 23  | The authenticated read forwards the cookie plus the ADR-0001 §6 fingerprint headers, and relays an incoming `x-forwarded-for`          | `apps/portal/lib/header-auth.ts:38`                                                                                                                                                                                                | `apps/doctor/lib/session.ts:60`                                                                                                                        | one bound-surface header set                                                                                 | none                                                                                                                                                                                                                                                                                                                                                                                            | `apps/doctor/lib/session.test.ts:41`, `:62`, `:119`; `apps/portal/lib/my-events.test.ts:207`                                                                                                               |
| 24  | With no session cookie no upstream authed read is issued at all — guest resolves locally                                               | `apps/portal/lib/header-auth.ts:38`                                                                                                                                                                                                | `apps/doctor/lib/shell-auth.ts:37`                                                                                                                     | one guest short-circuit                                                                                      | none                                                                                                                                                                                                                                                                                                                                                                                            | `apps/doctor/lib/session.test.ts:55`, `apps/doctor/lib/shell-auth.test.ts:26`                                                                                                                              |
| 25  | A 401 or an upstream failure on the shell auth read degrades to guest instead of taking the shell down                                 | `apps/portal/lib/header-auth.ts:57` `useHeaderAuth`, refreshed imperatively at `:53`                                                                                                                                               | `apps/doctor/lib/shell-auth.ts:37`, `:72`                                                                                                              | one degrade-to-guest rule; the shell auth-cluster slot consumes it                                           | none                                                                                                                                                                                                                                                                                                                                                                                            | `apps/doctor/lib/shell-auth.test.ts:50`, `:61`, `:69`; `apps/doctor/lib/session.test.ts:93`                                                                                                                |
| 26  | **Where the signed-in decision is taken on an auth route**                                                                             | CLIENT — the hook redirects an authenticated visitor off the surface: `apps/portal/lib/use-redirect-if-authenticated.ts:48`, wired by `apps/portal/components/auth-shell.tsx:41`                                                   | SERVER — the route resolves the landing and redirects before paint: `apps/doctor/app/(auth)/login/page.tsx:143` `redirect(landing)`                    | ONE authenticated-visitor guard for every auth route of both hosts                                           | `convergence: owner decision 2026-09-15 — «Ну очевидно, что надо убрать временный костыль и сделать полноценное перманентное серверное решение.»` — the doctor server mechanism wins: the package takes the signed-in decision server-side, before paint, in its route handling. `apps/portal/lib/use-redirect-if-authenticated.ts` is the named temporary hotfix and is DELETED in PR 1.4/1.5. | `apps/portal/components/auth-shell.test.tsx:59`, `apps/doctor/app/(auth)/login/page.test.tsx:86`, `apps/doctor/components/login-screen.test.tsx:54`; owed: a package server-redirect branch test in PR 1.4 |
| 27  | **An authenticated visitor is redirected off EVERY auth surface, with a named exemption list**                                         | `/login`, `/register`, `/verify` redirect to `/account`; `/reset` is exempt through `allowAuthenticated` — `apps/portal/components/auth-shell.tsx:41`                                                                              | no exemption list exists — `/reset` is a plain screen, `apps/doctor/app/(auth)/reset/page.tsx`                                                         | one guard plus a host-declared exemption path list                                                           | `config: routes.allowAuthenticated` — settled by the row-26 owner decision of 2026-09-15: the exemption list is host data read by the package's server-side redirect, not a client-side wiring.                                                                                                                                                                                                 | `apps/portal/components/auth-shell.test.tsx:91`, `apps/portal/e2e/auth-journeys.e2e.spec.ts:144`                                                                                                           |
| 28  | A guest still gets the door — the guard closes it to nobody else                                                                       | `apps/portal/components/auth-shell.tsx:41`                                                                                                                                                                                         | `apps/doctor/app/(auth)/login/page.tsx:125`                                                                                                            | same guard, guest branch                                                                                     | none                                                                                                                                                                                                                                                                                                                                                                                            | `apps/portal/components/auth-shell.test.tsx:77`, `apps/doctor/app/(auth)/login/page.test.tsx:129`, `apps/doctor/e2e/login-arrival.spec.ts:104`                                                             |
| 29  | The return target is parked when the visitor enters the auth flow and survives the interruption of the verification mail               | `apps/portal/middleware.ts:32` parks it, cookie written at `:38`; read/cleared by `apps/portal/lib/return-to-origin.ts:38`, `:45`, `:52`                                                                                           | no parking cookie — the target rides the canonical `returnTo` param, `apps/doctor/lib/return-context.ts:54` `RETURN_CONTEXT_PARAM`                     | ONE return-target codec; parking is a host-declared cookie, absent on the doctor host                        | `config: returnTo.parkingCookie` (`{name,maxAgeSeconds}`; `undefined` on the doctor host)                                                                                                                                                                                                                                                                                                       | `apps/portal/lib/return-to-origin.test.ts:30`, `apps/portal/e2e/return-to-origin.spec.ts:193`, `:206`                                                                                                      |
| 30  | The parked target is consumed exactly ONCE and then cleared                                                                            | `apps/portal/lib/return-to-origin.ts:45`                                                                                                                                                                                           | n/a — a param cannot go stale                                                                                                                          | same codec                                                                                                   | `config: returnTo.parkingCookie`                                                                                                                                                                                                                                                                                                                                                                | `apps/portal/lib/return-to-origin.test.ts:39`, `apps/portal/e2e/return-to-origin.spec.ts:230`                                                                                                              |
| 31  | A still-present query target wins over the parked one and clears it; a tampered or cross-origin target is dropped rather than followed | `apps/portal/lib/return-to-origin.ts:75`, `:89` `resolveReturnTarget`                                                                                                                                                              | `apps/doctor/lib/return-context.ts:133` `resolveReturnTargetPath`                                                                                      | one same-origin guard — `parseReturnTarget` in `@ds/schemas`                                                 | none                                                                                                                                                                                                                                                                                                                                                                                            | `apps/portal/lib/return-to-origin.test.ts:47`, `:55`, `:72`; `apps/portal/e2e/return-to-origin.spec.ts:255`; `apps/doctor/app/(auth)/login/page.test.tsx:121`                                              |
| 32  | **`/account` is a legal return-target shape (#1987)**                                                                                  | not admitted — `RETURN_TARGET_SHAPES` in `packages/schemas/src/events/registration-intent.ts` knows event shapes only, so a `returnTo=/account` login lands on `/webinars` (`apps/portal/e2e/shell/post-login-landing.spec.ts:30`) | explicitly excluded — the LD-4 landing union carries exactly two destinations and never the account page, `apps/doctor/lib/registration-landing.ts:72` | one target codec whose shape list gains `/account`; both hosts then land there after password AND code login | `convergence: #1987` (`config: routes.account` supplies the per-host path)                                                                                                                                                                                                                                                                                                                      | `apps/doctor/lib/registration-landing.test.ts:71` (the union must gain the account shape); owed: an Academy `parseReturnTarget('/account')` case in PR 1.4                                                 |

### 2.4 PR 1.5 — the sign-in door

| #   | Scenario                                                                                                                                                          | Academy behaviour (file:line)                                                                  | Doctor behaviour (file:line)                                                                                                                | Shared contract                                                                           | Allowed difference                                                                                                                                                          | Test id(s)                                                                                                                                                                                                                                                                                                           |
| --- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 33  | Password login starts without a CAPTCHA, then a stable backend challenge retries the original values exactly once                                                 | `apps/portal/app/login/page.tsx:85` `PortalLoginCard`                                          | `apps/doctor/components/login-screen.tsx:187` `LoginScreen`                                                                                 | ONE login composition over the shared `LoginCard` block                                   | none                                                                                                                                                                        | `apps/portal/app/login/page.test.tsx:143`                                                                                                                                                                                                                                                                            |
| 34  | The initial sign-in-code request waits for a fresh challenge token, the code confirmation stays challenge-free, and a resend mints a new challenge and sends once | `apps/portal/app/login/page.tsx:199` `onOtpRequest`, `:213` `onOtpResend`                      | `apps/doctor/components/login-screen.tsx:127` `otpRequestResolver`                                                                          | one challenge policy per action                                                           | none                                                                                                                                                                        | `apps/portal/app/login/page.test.tsx:177`, `:195`, `:210`                                                                                                                                                                                                                                                            |
| 35  | Switching sign-in method terminates the in-flight challenge — returning to the tab neither re-runs it nor replays the request                                     | `apps/portal/app/login/page.tsx:257` `onMethodChange`                                          | absent (flag off) — this host serves the password channel only                                                                              | one method-switch rule; the host declares the channels it serves                          | `config: channels`                                                                                                                                                          | `apps/portal/app/login/page.method-switch.test.tsx:114`, `:140`                                                                                                                                                                                                                                                      |
| 36  | The submit shows a spinner and `aria-busy` while the request is in flight                                                                                         | `apps/portal/app/login/page.tsx:85`                                                            | UNVERIFIED — no in-flight assertion exists on `apps/doctor/components/login-screen.tsx`                                                     | one in-flight state on the shared submit                                                  | none                                                                                                                                                                        | `apps/portal/app/login/page.test.tsx:255`, `:274`                                                                                                                                                                                                                                                                    |
| 37  | **A signed-in direct arrival never sees the form**                                                                                                                | the client hook sends it to `/account` — `apps/portal/lib/use-redirect-if-authenticated.ts:48` | the server resolves the LD-4 landing and redirects — `apps/doctor/app/(auth)/login/page.tsx:139`, `:143`                                    | one guard (row 26) plus a host landing table                                              | `convergence: owner decision 2026-09-15` (row 26) — the server resolves the landing and redirects before paint on both hosts; the Academy client hook is deleted in PR 1.5. | `apps/portal/components/auth-shell.test.tsx:59`, `apps/doctor/app/(auth)/login/page.test.tsx:86`, `apps/doctor/e2e/login-arrival.spec.ts:90`                                                                                                                                                                         |
| 38  | A signed-in doctor with a remembered specialty lands on the events feed; an unresolved read lands on the storefront home, never on a feed filtered by a guess     | absent (flag off) — this host has one default landing and no specialty memory                  | `apps/doctor/lib/registration-landing.ts:54`, `:62`, `:72` `resolveDirectArrivalLanding`                                                    | one landing resolver taking the ALREADY-READ specialty as data (see the §4 STOP question) | `config: landing.specialtyAware`                                                                                                                                            | `apps/doctor/lib/registration-landing.test.ts:42`, `:50`, `:64`; `apps/doctor/app/(auth)/login/page.test.tsx:93`; `apps/doctor/e2e/register-direct.spec.ts:116`, `:127`                                                                                                                                              |
| 39  | A signed-in GATE arrival goes to the эфир it came from, not the generic landing                                                                                   | `apps/portal/lib/registration-resume.ts:52` `completeReturnTarget`                             | `apps/doctor/app/(auth)/login/page.tsx:125`                                                                                                 | one carried-target resolver                                                               | none                                                                                                                                                                        | `apps/portal/lib/registration-resume.test.ts:35`, `apps/doctor/app/(auth)/login/page.test.tsx:109`, `apps/doctor/e2e/login-arrival.spec.ts:74`                                                                                                                                                                       |
| 40  | A hostile `returnTo` is never a redirect vector — the guard output lands, never the raw param                                                                     | `apps/portal/app/login/page.tsx:85`                                                            | `apps/doctor/lib/return-context.ts:133`                                                                                                     | one same-origin guard                                                                     | none                                                                                                                                                                        | `apps/portal/app/login/page.test.tsx:341`, `apps/portal/e2e/shell/post-login-landing.spec.ts:55`, `apps/doctor/app/(auth)/login/page.test.tsx:121`                                                                                                                                                                   |
| 41  | With no carried target login lands on the host default                                                                                                            | `/webinars`, the discovery listing — never the marketing landing                               | the LD-4 destination resolved by row 38                                                                                                     | one default-landing field                                                                 | `config: routes.defaultLanding`                                                                                                                                             | `apps/portal/e2e/shell/post-login-landing.spec.ts:30`, `apps/portal/lib/registration-resume.test.ts:55`, `apps/doctor/lib/registration-landing.test.ts:64`                                                                                                                                                           |
| 42  | On login success with a carried event context the system registers for that event and lands on its page — for password AND for code login                         | `apps/portal/app/login/page.tsx:85`                                                            | `apps/doctor/components/login-screen.tsx:187`                                                                                               | one post-login intent replay                                                              | none                                                                                                                                                                        | `apps/portal/app/login/page.test.tsx:304`, `:357`; `apps/doctor/components/login-screen.return.test.tsx:71`; `apps/doctor/e2e/event-register-one-tap.spec.ts:160`                                                                                                                                                    |
| 43  | A direct arrival, or a target the guard refuses, fires NO registration — the landing simply stands                                                                | `apps/portal/app/login/page.tsx:85`                                                            | `apps/doctor/components/login-screen.tsx:187`                                                                                               | same replay, negative branches                                                            | none                                                                                                                                                                        | `apps/portal/app/login/page.test.tsx:324`, `:341`; `apps/doctor/components/login-screen.return.test.tsx:81`, `:88`                                                                                                                                                                                                   |
| 44  | A rejected credential renders the error block on the door with no existence oracle                                                                                | `apps/portal/lib/auth-error-message.ts:31`                                                     | `apps/doctor/lib/auth-error-message.ts:49`                                                                                                  | the row-10 dictionary                                                                     | `config: copy`                                                                                                                                                              | `apps/portal/e2e/identifier-validation.e2e.spec.ts:91`, `apps/doctor/e2e/login.spec.ts:98`                                                                                                                                                                                                                           |
| 45  | The door's cross-links stay on THIS host and carry the arrival context onward: create-account into `/register`, «Забыли пароль» into `/reset`                     | `apps/portal/app/login/page.tsx:85`                                                            | `apps/doctor/components/login-screen.tsx:187`; recovery links to the storefront `/reset`, `apps/doctor/components/login-screen.test.tsx:40` | one link table fed by the host routes                                                     | `config: routes.register` / `routes.reset`                                                                                                                                  | `apps/portal/app/login/page.test.tsx:384`, `apps/doctor/components/login-screen.test.tsx:48`, `:40`; `apps/doctor/e2e/login.spec.ts:70`, `:79`                                                                                                                                                                       |
| 46  | Both doors publish the return context as a card beside the form; with none, NOTHING stands in for it (honest-empty), and exactly one render exists per viewport   | absent (flag off) — no return-context card on either Academy door                              | `apps/doctor/components/return-context-card.tsx:61`, `:102`, `:140`; resolved by `apps/doctor/lib/return-context.ts:166`                    | one card block; the two doors fork only the line                                          | `config: returnContext.card`                                                                                                                                                | `apps/doctor/components/return-context-card.test.tsx:36`, `:45`, `:54`; `apps/doctor/components/login-screen.test.tsx:60`; `apps/doctor/e2e/login-arrival.spec.ts:46`, `:63`; `apps/doctor/e2e/login-return-context.spec.ts:37`, `:58`; `apps/doctor/e2e/register-return-context.spec.ts:66`, `:101`, `:108`, `:120` |

### 2.5 PR 1.6 — the registration door, consents, hand-off and resume

| #   | Scenario                                                                                                                                                                                                                                                | Academy behaviour (file:line)                                                                                                                          | Doctor behaviour (file:line)                                                                                                                                       | Shared contract                                                                           | Allowed difference                                                                                                                                                                                                                                                                                                              | Test id(s)                                                                                                                                                                                             |
| --- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------------ | ----------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| 47  | The chromeless auth frame renders — wordmark, brand panel, card — and no site chrome appears anywhere on the route                                                                                                                                      | `apps/portal/components/auth-shell.tsx:41` `AuthShell`                                                                                                 | `apps/doctor/components/auth-shell.tsx`, mounted by `apps/doctor/app/(auth)/layout.tsx`                                                                            | ONE auth shell over the shared blocks; the host supplies wordmark and brand copy          | `config: copy`                                                                                                                                                                                                                                                                                                                  | `apps/portal/components/auth-shell.test.tsx:77`, `apps/doctor/e2e/register-layout.spec.ts:77`, `:166`, `:261`                                                                                          |
| 48  | Exactly three inputs — email, password, optional promo code                                                                                                                                                                                             | `apps/portal/app/register/page.tsx:83` `RegisterProjection` — no promo field on this host                                                              | `apps/doctor/lib/register-fields.ts:33`                                                                                                                            | one field set; the promo field is host-switched                                           | `config: register.promoField`                                                                                                                                                                                                                                                                                                   | `apps/doctor/e2e/register-layout.spec.ts:101`                                                                                                                                                          |
| 49  | The card is centred on the vertical axis, not pinned to the top, and at 390 the brand panel drops so the form column stands alone                                                                                                                       | `apps/portal/components/auth-shell.tsx:41`                                                                                                             | `apps/doctor/components/auth-shell.tsx`                                                                                                                            | one canvas — `design-source/auth.dc.html`                                                 | none                                                                                                                                                                                                                                                                                                                            | `apps/doctor/e2e/register-layout.spec.ts:236`, `:316`; owed: an Academy `apps/portal/app/register` layout e2e in PR 1.6                                                                                |
| 50  | An unfilled envelope slot is ABSENT from the tree, never an empty frame, and no document is requested in the empty, filled or error state                                                                                                               | `apps/portal/app/register/page.tsx:83`                                                                                                                 | `apps/doctor/components/registration-screen.tsx:148`                                                                                                               | one slot rule: absent beats empty                                                         | none                                                                                                                                                                                                                                                                                                                            | `apps/doctor/e2e/register-layout.spec.ts:181`, `:141`                                                                                                                                                  |
| 51  | A successful submit opens the confirmation state                                                                                                                                                                                                        | the host routes to the standalone `/verify` page, carrying the event context — `apps/portal/app/register/page.tsx:124` `onSubmit`                      | the confirmation is an INLINE step of the same screen — `apps/doctor/components/registration-screen.tsx:307`                                                       | one registration composition whose confirmation step is either a route or an inline panel | `config: routes.verify` (`undefined` = inline step)                                                                                                                                                                                                                                                                             | `apps/portal/app/register/page.test.tsx:217`, `apps/doctor/e2e/register-bot-protection.spec.ts:186`, `:234`                                                                                            |
| 52  | The creation password field carries the reveal toggle (#1393): masked by default, reveals and re-masks with the value intact, operable by keyboard alone, and the revealed state dies on reload                                                         | `apps/portal/app/register/page.tsx:171` `passwordRevealLabels`                                                                                         | `apps/doctor/lib/register-fields.ts:72` field, shared `PasswordField` primitive                                                                                    | one `PasswordField` primitive consumed by register, reset-complete and password login     | none                                                                                                                                                                                                                                                                                                                            | `apps/portal/e2e/password-reveal.e2e.spec.ts:107`, `apps/doctor/e2e/register-password-reveal.spec.ts:25`, `:49`                                                                                        |
| 53  | Every error is actionable in the field where it occurred and is programmatically tied to that field                                                                                                                                                     | `apps/portal/lib/identifier-validation.ts:55`                                                                                                          | `apps/doctor/lib/register-fields.ts:33`                                                                                                                            | one validation result shape carrying the field id                                         | none                                                                                                                                                                                                                                                                                                                            | `apps/portal/e2e/identifier-validation.e2e.spec.ts:284`, `apps/doctor/e2e/register-validation.spec.ts:64`, `apps/doctor/e2e/register-mobile.spec.ts:401`, `apps/doctor/lib/register-fields.test.ts:22` |
| 54  | The password error REPLACES the hint in the single shared slot, and the hint returns when the error clears                                                                                                                                              | `apps/portal/app/register/page.tsx:83`                                                                                                                 | `apps/doctor/e2e/register-validation.spec.ts:90`                                                                                                                   | one hint-OR-error slot                                                                    | `none` — the one-slot rule is settled (owner, 2026-09-06: the 021 spec clause proposing a second slot outlived its premise)                                                                                                                                                                                                     | `apps/doctor/e2e/register-validation.spec.ts:90`                                                                                                                                                       |
| 55  | A refused submit preserves every field value, every checkbox state and the resolved attribution, and offers a retry without re-filling                                                                                                                  | `apps/portal/app/register/page.tsx:124`                                                                                                                | `apps/doctor/components/registration-screen.tsx:410`                                                                                                               | one submit-failure rule: the form state outlives the command                              | none                                                                                                                                                                                                                                                                                                                            | `apps/portal/app/register/page.test.tsx:137`; UNVERIFIED for the checkbox + attribution half — `apps/doctor/e2e/register-network-failure.spec.ts` does not exist (#1550), owed in PR 1.6               |
| 56  | **Two-tier consents** — access conditions framed above the submit, marketing below it; neither pre-ticked; no flat list and no expandable disclosure                                                                                                    | absent (flag off) — ONE required consent line, `apps/portal/lib/consent.ts:11` `REQUIRED_CONSENT`, rendered at `apps/portal/app/register/page.tsx:192` | `apps/doctor/app/(auth)/register/page.tsx:122` `CONSENT_TIERS`, composed at `apps/doctor/components/registration-screen.tsx:165`                                   | one consent-tier renderer driven by a host-supplied tier list                             | `product divergence: host audience` — owner decision 2026-09-15: «Академия не для медработников — никакой декларации для них там быть не может. Про партнёрские данные тоже, так как сами партнёры там регистрируются. … всё верно — в состоянии выкл.» Carried as `config: consents.tiers`; the Academy stays off permanently. | `apps/doctor/e2e/register-consent-tiers.spec.ts:30`, `:120`, `:155`; `apps/doctor/e2e/register-consent-optional.spec.ts:68`                                                                            |
| 57  | **The medical-worker declaration** — renders with its legal explanation, is not pre-ticked, stands above the submit inside the form, blocks the submit with a reason that names it, and asks for no document                                            | absent (flag off)                                                                                                                                      | `apps/doctor/components/registration-screen.tsx:174` `MEDICAL_WORKER_DECLARATION_LABEL`                                                                            | one declaration block, host-switched                                                      | `product divergence: host audience` (owner decision 2026-09-15, row 56) — carried as `config: consents.medicalWorkerDeclaration`; the Academy stays off permanently.                                                                                                                                                            | `apps/doctor/e2e/register-medworker.spec.ts:50`, `:74`, `:94`, `:114`, `:141`; owed: a flag-branch unit test (on and off) in PR 1.6                                                                    |
| 58  | **The partner-data consent item** names the exact composition of what is shared and states that contacts are not                                                                                                                                        | absent (flag off)                                                                                                                                      | `apps/doctor/components/registration-screen.tsx:477` `partnerDataItem`                                                                                             | one consent-item renderer                                                                 | `product divergence: host audience` (owner decision 2026-09-15, row 56) — carried as `config: consents.partnerDataItem`; the Academy stays off permanently.                                                                                                                                                                     | `apps/doctor/e2e/register-consent-tiers.spec.ts:91`; owed: a flag-branch unit test (on and off) in PR 1.6                                                                                              |
| 59  | The optional marketing opt-in is unticked on load, is never named as a blocking reason, sends no consent entry when left alone and exactly one when ticked; withdrawal goes through a platform manager, with no self-service control contradicting that | absent (flag off) — the single Academy consent is mandatory                                                                                            | `apps/doctor/app/(auth)/register/page.tsx:122`                                                                                                                     | the same tier list, optional tier                                                         | `config: register.consentTiers`                                                                                                                                                                                                                                                                                                 | `apps/doctor/e2e/register-consent-optional.spec.ts:91`, `:110`, `:129`, `:153`, `:164`; `apps/doctor/e2e/register-bot-protection.spec.ts:124`                                                          |
| 60  | The submitted command carries every ticked access consent and the declaration                                                                                                                                                                           | `apps/portal/lib/consent.ts:11` — one entry                                                                                                            | `apps/doctor/lib/storefront-auth-client.ts:102` `registerDoctor`                                                                                                   | one consent payload builder over the tier list                                            | `config: register.consentTiers`                                                                                                                                                                                                                                                                                                 | `apps/doctor/e2e/register-bot-protection.spec.ts:93`                                                                                                                                                   |
| 61  | Partner attribution and the points promise are honest-empty SLOTS on the door — unfilled means absent, never a placeholder                                                                                                                              | absent (flag off)                                                                                                                                      | `apps/doctor/app/(auth)/register/page.tsx:84` — both deliberately UNSUPPLIED (#1544, #1545); slots at `apps/doctor/components/registration-screen.tsx:148`, `:150` | optional data slots in the host config, rendered only when supplied                       | `config: register.attribution` / `register.pointsPromise`                                                                                                                                                                                                                                                                       | `apps/doctor/e2e/register-layout.spec.ts:181`                                                                                                                                                          |
| 62  | On mobile every consent is a labelled control in the accessibility tree and the two tiers are separated in the tree, not by rendering alone                                                                                                             | absent (flag off) — one consent, no tiers                                                                                                              | `apps/doctor/components/registration-screen.tsx:165`                                                                                                               | the same renderer emits the tier semantics                                                | `config: register.consentTiers`                                                                                                                                                                                                                                                                                                 | `apps/doctor/e2e/register-mobile.spec.ts:368`, `:460`; owed: an Academy `apps/portal/app/register` mobile a11y e2e in PR 1.6                                                                           |
| 63  | A safe return target rides the hand-off into registration and reaches the confirmation step; an absent one leaves the navigation untouched and a cross-origin one is dropped at the hop                                                                 | `apps/portal/lib/registration-handoff.ts:34` `withReturnTarget`                                                                                        | `apps/doctor/lib/return-context.ts:151` `withReturnContext`                                                                                                        | ONE hand-off builder; the room and feed shapes come from `@ds/room` / `@ds/schemas`       | none                                                                                                                                                                                                                                                                                                                            | `apps/portal/lib/registration-handoff.test.ts:13`, `:23`, `:35`, `:42`, `:55`; `apps/doctor/e2e/register-return-handoff.spec.ts:93`, `:120`, `:147`                                                    |
| 64  | The parked target survives the whole trip, completes the carried registration, is consumed once, and an Academy-shaped target lands on the doctor host verbatim                                                                                         | `apps/portal/lib/registration-resume.ts:52` `completeReturnTarget`                                                                                     | `apps/doctor/lib/return-context.ts:195`, `:206`                                                                                                                    | one resume resolver over one shape list                                                   | none                                                                                                                                                                                                                                                                                                                            | `apps/portal/lib/registration-resume.test.ts:35`, `:46`, `:55`, `:61`, `:68`; `apps/doctor/lib/return-context.test.ts:195`, `:206`; `apps/doctor/e2e/events-guest.spec.ts:76`, `:115`                  |

### 2.6 PR 1.7 — the confirmation step

Blocked until Q1 and Q2 are answered: one post-throw exit and one cold-confirm exit ship, the other is deleted.

| #   | Scenario                                                                                                                                                                                     | Academy behaviour (file:line)                                                                                           | Doctor behaviour (file:line)                                                                                                                                                           | Shared contract                                                                      | Allowed difference                                                                                                                                                                                                                                                                       | Test id(s)                                                                                                                                                                                               |
| --- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------ | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 65  | The confirmation step seeds the destination from the URL — the `?email=` query, else the fragment — and the typed code reaches the api                                                       | `apps/portal/app/verify/page.tsx:89`                                                                                    | `apps/doctor/lib/storefront-auth-client.ts:114` `verifyEmail`; the step is inline and inherits the address just submitted                                                              | one confirmation composition; the deep-link entry is host-switched                   | `config: verify.deepLinkEntry`                                                                                                                                                                                                                                                           | `apps/portal/app/verify/page.test.tsx:413`, `apps/doctor/lib/storefront-auth-client.test.ts:122`                                                                                                         |
| 66  | On a successful confirm with a HELD password the login is replayed before the success state, and the password lives only in the volatile in-memory envelope — never a URL, storage or cookie | `apps/portal/app/verify/page.tsx:204`                                                                                   | `apps/doctor/components/registration-screen.tsx:307`; the hold is written only AFTER the registration succeeded (`:396`) and a stale hold is dropped at the top of the submit (`:410`) | ONE held-password slot — `packages/design-system/src/blocks/pending-registration.ts` | none                                                                                                                                                                                                                                                                                     | `apps/portal/app/verify/page.test.tsx:342`, `apps/doctor/components/registration-screen.test.tsx:307`, `:396`, `:410`                                                                                    |
| 67  | **The replay is refused or throws**                                                                                                                                                          | stays on the confirmation step with the generic EARS-16 error — `apps/portal/app/verify/page.tsx:226`                   | routes to the sign-in door with the return context, slot wiped, no success card — `apps/doctor/components/registration-screen.tsx:368`                                                 | ONE post-throw exit                                                                  | `convergence: owner decision 2026-09-15 — «1. Как в Академии»` — both hosts stay on the confirmation step with the generic EARS-16 error. The doctor door-routing branch is deleted in PR 1.7, which thereby promotes and closes the `DEBT.md:152` divergence.                           | `apps/portal/app/verify/page.test.tsx:446`; `apps/doctor/components/registration-screen.test.tsx:368` and `apps/doctor/e2e/register-return.spec.ts:212` are REWRITTEN to the stay-on-step rule in PR 1.7 |
| 68  | **A cold confirmation step — no held password**                                                                                                                                              | routes to `/login`, carrying the event context onward — `apps/portal/app/verify/page.tsx:225`                           | routes to the sign-in door with the return context — `apps/doctor/components/registration-screen.tsx:342`                                                                              | ONE cold-confirm exit                                                                | `convergence: owner decision 2026-09-15 — «2. Как в Академии»` — the cold confirmation step routes to `/login` on both hosts, carrying the context onward. #1394 («keep the code field and the resend alive») is thereby REJECTED by owner decision 2026-09-15; no code follows from it. | `apps/portal/app/verify/page.test.tsx:431`, `:360`; `apps/doctor/components/registration-screen.test.tsx:342` is rewritten to the `/login` exit in PR 1.7                                                |
| 69  | On a successful confirm with a carried эфир the system registers for it before the success state and lands on its page, signed in                                                            | `apps/portal/app/verify/page.tsx:204`                                                                                   | `apps/doctor/components/registration-screen.tsx:307`                                                                                                                                   | one post-confirm intent replay                                                       | none                                                                                                                                                                                                                                                                                     | `apps/portal/app/verify/page.test.tsx:342`, `apps/doctor/components/registration-screen.test.tsx:224`, `apps/doctor/e2e/register-return.spec.ts:172`                                                     |
| 70  | **The confirmation step on a reload, a restored tab or an expired hold**                                                                                                                     | a cold step bounces to `/login` and the progress is gone — `apps/portal/app/verify/page.test.tsx:431`                   | UNVERIFIED — the inline step dies with the client state; no reload test exists on this host                                                                                            | ONE survival rule for the step                                                       | `convergence: owner decision 2026-09-15 — «2. Как в Академии»` (row 68) — a cold step bounces to `/login` on both hosts; #1394 is rejected.                                                                                                                                              | `apps/portal/app/verify/page.test.tsx:431`; owed: a doctor reload/restored-tab case in PR 1.7                                                                                                            |
| 71  | A direct arrival carries no эфир, and a refused registration never strands the doctor — the success state still stands                                                                       | `apps/portal/app/verify/page.test.tsx:375` — a cross-origin target lands on the discovery listing and registers nothing | `apps/doctor/components/registration-screen.tsx:307`                                                                                                                                   | same replay, negative branches                                                       | none                                                                                                                                                                                                                                                                                     | `apps/portal/app/verify/page.test.tsx:375`, `apps/doctor/components/registration-screen.test.tsx:274`, `:288`                                                                                            |
| 72  | Resend is disabled during the cooldown, re-enables after it, hits the EARS-25 endpoint and re-arms                                                                                           | `apps/portal/app/verify/page.tsx:89`                                                                                    | `apps/doctor/lib/storefront-auth-client.ts:123` `resendVerification`                                                                                                                   | one cooldown timer and one resend endpoint                                           | none                                                                                                                                                                                                                                                                                     | `apps/portal/app/verify/page.test.tsx:199`, `:159`; `apps/doctor/lib/storefront-auth-client.test.ts:108`; `apps/doctor/e2e/register-bot-protection.spec.ts:186`                                          |
| 73  | The resend acknowledgement is the SAME regardless of the address — no existence branch (#326)                                                                                                | `apps/portal/app/verify/page.test.tsx:242`                                                                              | `apps/doctor/lib/storefront-auth-client.ts:123`                                                                                                                                        | one existence-agnostic acknowledgement                                               | none                                                                                                                                                                                                                                                                                     | `apps/portal/app/verify/page.test.tsx:242`; owed: `apps/doctor/e2e/register-existing.spec.ts` does not exist (#1549), PR 1.7                                                                             |
| 74  | Resend is hidden when there is no address to target (a bare deep-link entry)                                                                                                                 | `apps/portal/app/verify/page.test.tsx:275`                                                                              | absent (flag off) — the inline step always knows the address it just submitted                                                                                                         | the same rule, reachable only where deep-link entry is served                        | `config: verify.deepLinkEntry`                                                                                                                                                                                                                                                           | `apps/portal/app/verify/page.test.tsx:275`                                                                                                                                                               |
| 75  | **The fixed-length code auto-submits with no manual click; a submit blocked for want of an identifier surfaces a VISIBLE error, never a silent no-op**                                       | the submit stays enabled and states the reason on the attempt — `apps/portal/app/verify/page.tsx:244`                   | the submit is inert with its reason wired to the control — `apps/doctor/lib/register-fields.ts:102`                                                                                    | one submit-gating rule (row 16)                                                      | `convergence: owner decision 2026-09-15 — «Больше склоняюсь к тому, как это в Академии»` (row 16) — the submit stays enabled and the attempted submit surfaces the missing identifier as a visible field error with focus.                                                               | `apps/portal/app/verify/page.test.tsx:284`, `:446`; owed: `apps/doctor/e2e/register-validation.spec.ts:162` and `apps/doctor/e2e/register-layout.spec.ts:204` are rewritten in PR 1.6                    |
| 76  | BOTH co-equal paths stand on the step: the code form AND the sign-in / password-recovery actions, carrying the event context onward (LD-2)                                                   | `apps/portal/app/verify/page.tsx:89`                                                                                    | absent (flag off) — the inline step offers no door beside it                                                                                                                           | the co-equal actions belong to the standalone confirmation route                     | `config: routes.verify` (row 51)                                                                                                                                                                                                                                                         | `apps/portal/app/verify/page.test.tsx:190`, `:389`                                                                                                                                                       |
| 77  | A duplicate registration reaches an existence-agnostic screen with an account-exists notice and no code                                                                                      | `apps/portal/e2e/auth-journeys.e2e.spec.ts:371`                                                                         | `apps/doctor/lib/storefront-auth-client.ts:102`                                                                                                                                        | one duplicate-registration outcome                                                   | none                                                                                                                                                                                                                                                                                     | `apps/portal/e2e/auth-journeys.e2e.spec.ts:371`; owed: `apps/doctor/e2e/register-existing.spec.ts` (#1549), PR 1.7                                                                                       |

### 2.7 PR 1.8 — password recovery

| #   | Scenario                                                                                                                                                                                                               | Academy behaviour (file:line)                                                                   | Doctor behaviour (file:line)                                                | Shared contract                                                | Allowed difference                                    | Test id(s)                                                                                                                                                                                                                    |
| --- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------- | -------------------------------------------------------------- | ----------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 78  | The first paint is the shared password-recovery card in this host's RU copy, with its back link pointing at THIS host's sign-in                                                                                        | `apps/portal/app/reset/page.tsx:155`; the route is the `allowAuthenticated` exemption of row 27 | `apps/doctor/components/reset-screen.tsx:157` `ResetScreen`                 | ONE recovery composition over the shared card                  | `config: copy` / `config: routes.login`               | `apps/doctor/components/reset-screen.test.tsx:93`, `apps/portal/components/auth-shell.test.tsx:91`                                                                                                                            |
| 79  | The initiate step POSTs the typed identifier through the host BFF client and waits for a fresh challenge token                                                                                                         | `apps/portal/app/reset/page.tsx:155` `onRequest`                                                | `apps/doctor/lib/auth-client.ts:176`                                        | one initiate command                                           | none                                                  | `apps/portal/app/reset/page.test.tsx:111`, `apps/doctor/components/reset-screen.test.tsx:105`, `apps/doctor/lib/auth-client.test.ts:105`                                                                                      |
| 80  | An UNKNOWN identifier reaches the very same code step — the screen discloses no existence — and the resend acknowledgement is identical regardless of the identifier (#326)                                            | `apps/portal/app/reset/page.tsx:155`                                                            | `apps/doctor/components/reset-screen.tsx:157`                               | one existence-agnostic recovery flow                           | none                                                  | `apps/portal/app/reset/page.test.tsx:227`, `apps/doctor/components/reset-screen.test.tsx:117`, `:198`                                                                                                                         |
| 81  | Resend is disabled during the cooldown, then re-enables and re-calls the REAL initiate                                                                                                                                 | `apps/portal/app/reset/page.tsx:155`                                                            | `apps/doctor/components/reset-screen.tsx:157`                               | the row-72 cooldown timer                                      | none                                                  | `apps/portal/app/reset/page.test.tsx:172`, `apps/doctor/components/reset-screen.test.tsx:198`                                                                                                                                 |
| 82  | Completion sends the code with the HELD identifier and stays challenge-free; the new-password field carries the same reveal toggle (#1393)                                                                             | `apps/portal/app/reset/page.tsx:219` `passwordReveal`                                           | `apps/doctor/lib/auth-client.ts:189`                                        | one completion command; one `PasswordField` primitive (row 52) | none                                                  | `apps/portal/app/reset/page.test.tsx:285`, `apps/doctor/components/reset-screen.test.tsx:133`, `apps/doctor/lib/auth-client.test.ts:119`; owed: neither host has a reset-complete REVEAL e2e — #1393's third consumer, PR 1.8 |
| 83  | A completed reset leaves the doctor SIGNED IN on this host's account page, never back at `/login`                                                                                                                      | `apps/portal/app/reset/page.test.tsx:318` — routes to `/account` (003 EARS-12)                  | `apps/doctor/components/reset-screen.test.tsx:133` — this host's `/account` | one post-reset landing field                                   | `config: routes.account` (the same field row 32 adds) | `apps/portal/app/reset/page.test.tsx:318`, `apps/doctor/components/reset-screen.test.tsx:133`                                                                                                                                 |
| 84  | A refused initiate or a refused completion keeps the doctor on its step with the host's RU sentence; a too-short new password renders the RU length copy, never the baked English, and a class-free 8+ one is accepted | `apps/portal/app/reset/page.tsx:155`                                                            | `apps/doctor/components/reset-screen.tsx:157`                               | the row-10 dictionary plus one password rule                   | `config: copy`                                        | `apps/portal/e2e/auth-journeys.e2e.spec.ts:318`, `apps/portal/e2e/identifier-validation.e2e.spec.ts:253`, `:270`; `apps/doctor/components/reset-screen.test.tsx:158`, `:182`                                                  |
| 85  | «Начать заново» returns to the request step with an empty box, dropping the held identifier                                                                                                                            | `apps/portal/app/reset/page.tsx:187` `onRestart`                                                | `apps/doctor/components/reset-screen.tsx:157`                               | one restart action                                             | none                                                  | `apps/portal/app/reset/page.test.tsx:265`, `apps/doctor/components/reset-screen.test.tsx:238`                                                                                                                                 |

### 2.8 Out of the matrix

Auth-adjacent units that wave 1 does NOT move, recorded so a later reader does not re-open them:

- **Theme.** #2180 delivered PR 1.2 of the §4 sequence into `packages/storefront-shell`; `apps/doctor/components/theme-toggle.tsx` is already deleted. `apps/doctor/lib/theme.ts:22` and `apps/portal/lib/theme.test.ts` are the last host copies and travel with the shell wave, not wave 1. No matrix rows.
- **`apps/doctor/components/account-screen.tsx`** — wave 4 (`packages/account`). Only its two auth-adjacent assertions are cited above: recovery stays on this host (`apps/doctor/components/account-screen.test.tsx:115`) and a read that stays 401 sends the doctor to the door with the return context (`:144`).
- **`apps/doctor/lib/room.ts`** and **`apps/portal/lib/registration-state.ts`** (`resolveJoinSignpost:52`, `resolveRoomEntryHref:88`) belong to `@ds/room` and waves 3–4; only `room-return.ts` moves in PR 1.1 (§2.1).
- **`packages/design-system` auth blocks** (rows 41–44 of the registry) stay where they are — wave 1 owns compositions, not cards.

**Rows owing a test** (the feed for sections 3 and 5, each naming the PR that delivers it): 13 (PR 1.3) · 32 (PR 1.4) · 49, 55, 57, 58, 62 (PR 1.6) · 70, 73, 77 (PR 1.7) · 82 (PR 1.8). **`OPEN — owner Stage-A` rows: none** — the eleven that were open (16, 26, 27, 37, 56, 57, 58, 67, 68, 70, 75) are settled by the owner decisions of 2026-09-15 recorded in section 5. **`UNVERIFIED` cells remaining**: 36 (no doctor counterpart measured), 70 (no doctor reload test exists) — both are statements of absent evidence, and each is discharged by the owed test named in its row.

## 3. EARS disposition of the absorbed Issues

Format: `2026-09-07-one-code-two-storefronts-plan-en.md` §3 rule 4 — every shall-clause of an absorbed Issue is **covered** (a package test id), a **host difference** (a config flag plus the test of that branch), **rewritten** (an owner decision replaces the clause) or **cancelled/rejected** (an owner decision, quoted). No clause may be left unlisted: that is the whole point of collapsing 021 into one wave. Anchors are file:line as of `553ef3be`; a cell reading `UNVERIFIED — <why>` states absent evidence and is never a guess.

Absorbed here: **#1548**, **#1549**, **#1550** (021 EARS-12/13/14, closed as duplicates of this extraction), **#1987**, **#2001**, **#1393**, **#1394**.

### 3.1 #1548 — 021 EARS-12 (error actionability and the submit control)

| Clause                                                                                                 | Disposition                                     | Where it lands                                                                                                                                                                                                         | Matrix row |
| ------------------------------------------------------------------------------------------------------ | ----------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------- |
| «Every error is actionable in the field where it occurred … programmatically tied to its field»        | covered                                         | package test, moved from `apps/doctor/lib/register-fields.test.ts:22`, `:32` (the field rules) and `apps/portal/e2e/identifier-validation.e2e.spec.ts:284` (the blur-time binding on the Academy host)                 | 9, 53      |
| «While a mandatory condition is unmet the submit control is disabled with its reason stated beside it» | **rewritten by owner decision 2026-09-15 (Q4)** | superseded: the submit stays ENABLED and the attempted submit reveals every field error, captcha among them, with focus on the first. #1952 (`Button` inert-with-reason) is NOT adopted by wave 1. Section 5, Q4.      | 16, 75     |
| «A silently dead button exists in no state of this surface»                                            | covered                                         | `apps/portal/app/verify/page.test.tsx:446` («surfaces a VISIBLE error (never a silent no-op) when a submit is blocked with no identifier») moves into the package; the doctor inert-submit e2e are rewritten in PR 1.6 | 16, 75     |

### 3.2 #1549 — 021 EARS-13 (the existence-agnostic confirmation step)

| Clause                                                                          | Disposition                                          | Where it lands                                                                                                                                                                                                                                            | Matrix row |
| ------------------------------------------------------------------------------- | ---------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------- |
| «An identical body, status and timing whether or not the address is registered» | covered                                              | `apps/portal/e2e/auth-journeys.e2e.spec.ts:371` («EARS-23/24: duplicate register → existence-agnostic screen + account-exists notice (no code)») moves with the package; the doctor branch is owed as `apps/doctor/e2e/register-existing.spec.ts`, PR 1.7 | 73, 77     |
| «Code entry and «Войти» / «Восстановить пароль» stay co-equal per LD-2»         | **host difference** → `config: routes.{login,reset}` | the Academy has it today (`apps/portal/app/verify/page.test.tsx:190`); the doctor inline step has no such pair, so the package renders the actions its host config supplies paths for                                                                     | 76         |
| «No «этот email уже занят» dead end»                                            | covered                                              | `apps/portal/app/verify/page.test.tsx:242` («the resend confirmation is the SAME regardless of the email (no existence branch)») moves into the package                                                                                                   | 73         |

### 3.3 #1550 — 021 EARS-14 (a failed submit loses nothing)

| Clause                                                                                        | Disposition                                        | Where it lands                                                                                                                                                                                                                                | Matrix row |
| --------------------------------------------------------------------------------------------- | -------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------- |
| «Preserves every field value, every checkbox state and the resolved attribution»              | covered in part; checkbox + attribution UNVERIFIED | `apps/portal/app/register/page.test.tsx:137` covers the field-value half. The checkbox and attribution half has no test on either host — `apps/doctor/e2e/register-network-failure.spec.ts` does not exist; owed in PR 1.6                    | 55         |
| «A Russian failure message and a retry without re-filling»                                    | covered                                            | same test; the message text arrives through the one error dictionary moved in PR 1.3                                                                                                                                                          | 19, 55     |
| «The password lives only in the volatile in-memory envelope — never a URL, storage or cookie» | covered                                            | `apps/doctor/components/registration-screen.test.tsx:396` (held only after success) and `:410` (a stale hold is dropped at the top of the submit) become package tests; ONE slot, `packages/design-system/src/blocks/pending-registration.ts` | 66         |

### 3.4 #1987 — `returnTo=/account` (OPEN, closed by this wave)

| Clause                                                                        | Disposition                                    | Where it lands                                                                                                                                                                                                                                           | Matrix row |
| ----------------------------------------------------------------------------- | ---------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------- |
| «`parseReturnTarget('/account')` returns a same-origin `/account` target»     | covered by new work in PR 1.4                  | `RETURN_TARGET_SHAPES` in `packages/schemas/src/events/registration-intent.ts` admits event shapes only today, so the shape is ADDED with its own test; owed: an Academy `parseReturnTarget('/account')` case in PR 1.4                                  | 32         |
| «Both hosts land on `/account` after a password login AND after an OTP login» | **host difference** → `config: routes.account` | the destination union at `apps/doctor/lib/registration-landing.test.ts:71` («the account page is never a landing — the union carries exactly the two LD-4 destinations») is rewritten in PR 1.4 to admit the account shape; the path itself is host data | 32, 41     |

### 3.5 #2001 — one error dictionary on the confirmation step (OPEN, closed by this wave)

| Clause                                                                                                                               | Disposition                                                                                                                      | Where it lands                                                                                                                                                                                              | Matrix row |
| ------------------------------------------------------------------------------------------------------------------------------------ | -------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------- |
| «The confirmation-step submit maps through `authErrorMessage`: 429 → too-many-attempts, 5xx/network → unavailable, else the generic» | **convergence on the shipped Academy behaviour** — owner chat 2026-09-07, «лимит попыток должен звучать иначе, чем неверный код» | the Academy dictionary (`apps/portal/lib/auth-error-message.ts:31`, 429 already mapped at `:37`) becomes the package's one dictionary in PR 1.3; the doctor confirm-step 429 branch test is owed in that PR | 12, 13     |
| «The same mapping on the resend action»                                                                                              | covered by construction                                                                                                          | `apps/doctor/lib/storefront-auth-client.test.ts:108` — the resend already routes through the same client                                                                                                    | 20, 72     |
| «A wrong code still shows the generic sentence»                                                                                      | covered                                                                                                                          | `apps/doctor/lib/auth-error-message.test.ts:19` («401 and 400 stay the caller neutral generic (no existence oracle)») becomes a package test                                                                | 10, 11     |

### 3.6 #1393 — 003 EARS-38, the show-password toggle (absorbed into wave 1)

| Clause                                                                                         | Disposition                        | Where it lands                                                                                                                                                                                                                                                  | Matrix row |
| ---------------------------------------------------------------------------------------------- | ---------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------- |
| «The `PasswordField` primitive is consumed by register, reset-complete and the password login» | covered for two of three consumers | `/login` and `/register` carry the toggle on both hosts — `apps/portal/e2e/password-reveal.e2e.spec.ts:60` (`/login`), `:84` (keyboard), `:107` (`/register`); these move with the package                                                                      | 52, 82     |
| «…including the reset-complete field»                                                          | covered in code, UNVERIFIED in e2e | implemented at `apps/portal/app/reset/page.tsx:219` (`passwordReveal`) and unit-tested (`apps/portal/app/reset/page.test.tsx:285`, `apps/doctor/components/reset-screen.test.tsx:133`), but NO reset-complete reveal e2e exists on either host — owed in PR 1.8 | 82         |

### 3.7 #1394 — 003 EARS-39, keeping the confirmation step alive (absorbed, then rejected)

| Clause                                                                                                                      | Disposition                                             | Where it lands                                                                                                                                                                                                                                   | Matrix row |
| --------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | ---------- |
| «Keep the verification step — the code field and the EARS-25 resend — alive on a reload, a restored tab or an expired hold» | **rejected by owner decision 2026-09-15 (Q2)**          | «2. Как в Академии»: a cold step routes to `/login` with the context carried, which is the shipped Academy behaviour (`apps/portal/app/verify/page.test.tsx:431`). The clause REVERSED that behaviour, so no code follows from it. Section 5, Q2 | 68, 70     |
| «On success, route to sign-in with explicit localized copy about the just-created password»                                 | **rejected by owner decision 2026-09-15 (Q2)**          | same decision — the replay-and-land path stands as shipped; no sign-in bounce copy is added                                                                                                                                                      | 66, 69     |
| «Never bounce to `/login`, never discard progress; the password stays volatile-memory-only»                                 | split: volatile half **covered**, the rest **rejected** | the volatile-memory half is already covered (§3.3, `registration-screen.test.tsx:396`, `:410`) and stays. «Never bounce» is rejected with the clause above, which also settles the refused/throwing replay (Q1, «1. Как в Академии»)             | 66, 67, 68 |

**No clause of the seven absorbed Issues is unlisted.** #1548, #1549 and #1550 are closed as duplicates of this extraction; #1987 and #2001 close with PR 1.4 and PR 1.3; #1393 closes with PR 1.8; #1394 is closed as rejected with the Q2 quote in section 5.

## 4. PR sequence of wave 1

Eight PRs, in this order. Each one is a complete slice of the four wave steps of the plan's §4 (`(a)` Academy into the package, `(b)` doctor consumes and its copy is deleted, `(c)` registry row + allowlist in the SAME PR, `(d)` no `mirror-of` markers left). Step (d) is satisfied by construction for the whole wave — `grep -rn "mirror-of: " apps/doctor` returns nothing today (§1) — so no PR below owes a marker deletion, and none may introduce one.

**The allowed intermediate state is the same shape after every PR:** the capability that PR moved is mounted from `@ds/auth-flow` by both hosts, or by the Academy alone where the row says so; a capability a LATER PR moves still lives in its host file and that is legitimate until its own PR. What is never allowed between PRs: a re-export shim left behind in `apps/portal`, a host-local substitute for a rule the package already owns, or a divergence expressible neither as a `HostConfig` field nor as a section-5 decision.

| PR      | Moves from `apps/portal` → package                                                                                                                                        | Deletes from `apps/doctor`                                                                                                                  | Doctor switches | Matrix rows | Stage-B |
| ------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------- | --------------- | ----------- | ------- |
| **1.1** | `lib/room-return.ts` + its test → **`@ds/room`** (not `@ds/auth-flow`)                                                                                                    | none                                                                                                                                        | no              | 1–5         | —       |
| **1.2** | theme — **already delivered by #2180** into `packages/storefront-shell`; kept only so the numbering matches the plan's §4 sequence                                        | `components/theme-toggle.tsx` (already gone); `lib/theme.ts:22` is the last host copy and goes with the shell wave                          | done            | none (§2.8) | done    |
| **1.3** | `lib/{auth-client,auth-error-message,bot-protection,identifier-validation}.ts`                                                                                            | `lib/{auth-client,storefront-auth-client,auth-error-message,bot-protection,register-fields}.ts`                                             | yes             | 6–21        | —       |
| **1.4** | `lib/{header-auth,return-to-origin}.ts` + the parking rule of `middleware.ts` → `@ds/auth-flow/server`; `lib/use-redirect-if-authenticated.ts` is DELETED, not moved (Q3) | `lib/{session,shell-auth}.ts`                                                                                                               | yes             | 22–32       | both    |
| **1.5** | `app/login/page.tsx`                                                                                                                                                      | `app/(auth)/login/page.tsx`, `components/{login-screen,auth-shell,return-context-card}.tsx`, `lib/{registration-landing,return-context}.ts` | yes             | 33–46       | both    |
| **1.6** | `app/register/page.tsx`, `lib/{consent,registration-handoff,registration-resume}.ts`, `components/auth-shell.tsx`                                                         | `app/(auth)/register/page.tsx`, `components/registration-screen.tsx`                                                                        | yes             | 47–64       | both    |
| **1.7** | `app/verify/page.tsx`                                                                                                                                                     | the confirmation step inside `components/registration-screen.tsx`                                                                           | yes             | 65–77       | both    |
| **1.8** | `app/reset/page.tsx`                                                                                                                                                      | `app/(auth)/reset/page.tsx`, `components/reset-screen.tsx`, `lib/registration-success.ts`                                                   | yes             | 78–85       | both    |

### 4.1 What each PR still leaves standing

- **After 1.1** — `@ds/room` owns the room-return codec; `apps/portal/lib/{registration-handoff,registration-resume,registration-state}.ts` and `apps/portal/app/webinars/[slug]/room/room-routes.ts` import it in the SAME PR. No re-export shim outlives this PR. The doctor host is untouched. Registry step (c): the host-file allowlist row at `apps/docs/content/specs/product/two-site-ia/capability-ownership.md:195` still reads «wave 4 source (#2073) — moves to `packages/account` (room half)» and is rewritten to «wave 1 (#2027) → `@ds/room`» **in PR 1.1, not in this docs slice**; the plan spec wins because its 2026-09-08 import-graph measurement put `room-return` in wave 1. Must stay green: `apps/portal/e2e/return-to-origin.spec.ts:193`, `:206`, `:230`, `:255`.
- **After 1.3** — both hosts speak one error dictionary, one auth client, one bot-protection unit and one identifier rule set. #2001 closes here. Both hosts still own their own pages; the doctor register route stays `deferred` in `tools/lint/prod-surface-manifest.yaml`. Must stay green: `apps/portal/e2e/identifier-validation.e2e.spec.ts` (all nine cases) and the doctor `register-bot-protection` / `register-validation` specs.
- **After 1.4** — the signed-in decision is taken ONCE, server-side, inside the package (Q3); `apps/portal/lib/use-redirect-if-authenticated.ts` is gone and no client hook replaces it. `apps/portal/middleware.ts` is one package call plus its config. `RETURN_TARGET_SHAPES` admits `/account`, closing #1987. Both `/login` routes are still host pages, so the redirect lands on a host-owned surface — that is the allowed intermediate state, and it is why 1.4 already needs Stage-B on both hosts. Must stay green: `apps/portal/e2e/auth-journeys.e2e.spec.ts:144`, `shell/post-login-landing.spec.ts:30`, `:55`, the doctor `login-arrival` spec.
- **After 1.5** — one sign-in door. Both `/login` rows flip from `wave 1 (#2027)` to `mounted` in the route-file registry, so each route file is a mount plus its host config and nothing else (`pnpm lint:route-mount`). The doctor return-context card becomes `config: returnContext.card`. `/register`, `/verify` and `/reset` are still host pages on both hosts.
- **After 1.6** — one registration door. The consent tiers, the medical-worker declaration and the partner-data item are host config with the Academy off (Q5). The doctor register route may leave `deferred` in the prod-surface manifest ONLY once its submit is live — the honest-empty attribution and points slots stay unfilled, which is data, not a stub. The confirmation step still lives inline in `components/registration-screen.tsx` on the doctor host until 1.7; that is the last allowed twin.
- **After 1.7** — one confirmation step. The refused/throwing replay stays on the step (Q1) and the cold step routes to `/login` (Q2) on BOTH hosts, so the doctor door-routing branch and the `DEBT.md:152` divergence are deleted and promoted in this PR. #1394 is closed as rejected here.
- **After 1.8** — one password-recovery flow; `apps/doctor` holds no auth surface of its own. The §4 wave-exit criteria are run in this PR: `pnpm build` for both hosts, `pnpm ci:standalone-boot portal` and `… doctor`, `@ds/auth-flow` present in both `transpilePackages` and both `globals.css` `@source` lists, the axe + mobile-parity specs of every touched route green on both hosts, and the rollback recorded as the revert of the squash commit.

### 4.2 Draft `HostConfig`

Data only. Every field below is a value the host states about itself; a divergence not expressible here needs an ADR-backed product reason recorded before implementation (plan §2, ADR-0013 §A1 unchanged). `lint:import-boundary` type-checks each host's `host-config.ts` against this type with `satisfies` and rejects function-valued fields outside the closed adapter list of §4.3.

```ts
export type AuthFlowHostConfig = {
  /** Route table. `undefined` = this host does not serve that surface. */
  routes: {
    login: string;
    register: string;
    verify: string;
    reset: string;
    account: string; // #1987 — the account shape the return-target codec admits
    eventPathTemplate?: string; // data, not a callback: "/webinars/{slug}"
    defaultLanding: string;
    /** Q3 — auth paths an AUTHENTICATED visitor may still see. Read server-side. */
    allowAuthenticated: readonly string[];
  };
  /** Copy source: a messages object. Academy passes next-intl messages, doctor its RU literals. */
  copy: AuthFlowMessages;
  /** Data endpoint the flow talks to, relative so the __Host- cookie rides along (row 6). */
  api: {
    basePath: string; // e.g. "/v1/auth"
    registerPath: string; // "/v1/auth/register" vs the doctor storefront path
  };
  /** Q5 — owner decision 2026-09-15; the Academy sets all three off, permanently. */
  consents: {
    tiers: readonly ConsentTier[]; // [] = the single required consent line
    medicalWorkerDeclaration: boolean;
    partnerDataItem: boolean;
    marketingOptIn: boolean;
  };
  brand: {
    wordmark: BrandAsset;
    panel?: BrandAsset; // absent = the chromeless frame renders without the brand panel
  };
  /** Q3 — the landing table: where a signed-in or just-authenticated visitor is sent. */
  landing: {
    afterLogin: string;
    afterRegister: string;
    afterReset: string;
    specialtyAware: boolean; // doctor only; the package receives the specialty as DATA
  };
  /** returnTo parking. `undefined` = this host parks nothing. */
  returnTo?: {
    parkingCookie: { name: string; maxAgeSeconds: number };
    card: boolean; // the doctor return-context card
  };
  botProtection: { siteKeyEnv: string };
  channels: readonly OtpChannel[];
  register: {
    promoField: boolean;
    attribution?: AttributionSlot; // honest-empty: unfilled means ABSENT, never a placeholder
    pointsPromise?: PointsPromiseSlot;
  };
  verify: { deepLinkEntry: boolean };
};
```

### 4.3 Closed adapter list

Plan §2 allows a host to contribute a route file and a host-config object and nothing else, and `local/host-config-boundary` rejects function-valued config fields outside a closed list. Wave 1 declares that list **empty**: `AuthFlowHostConfig` above has no function-valued field. Three call sites looked like callbacks during the survey and are resolved as follows, so none of them reopens the list.

| Host mechanism today                                                         | Looks like                   | Wave-1 resolution                                                                                                                                                                                |
| ---------------------------------------------------------------------------- | ---------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| `resolveDirectArrivalLanding` — `apps/doctor/lib/registration-landing.ts:72` | a landing resolver callback  | `landing.specialtyAware: boolean` plus a package resolver that takes the already-read specialty as DATA. The host reads its own specialty; it does not hand the package a function.              |
| `resolveReturnContext` — `apps/doctor/lib/return-context.ts:166`             | a config-supplied api read   | a package **server helper**, not a config field — the read belongs to `@ds/auth-flow/server` with the endpoint named by `api.basePath`.                                                          |
| `refreshHeaderAuth` — `apps/portal/lib/header-auth.ts:53`                    | an imperative client refresh | belongs to the `@ds/storefront-shell` auth-cluster slot contract (#2180), not to `AuthFlowHostConfig`; PR 1.4 hands it to the shell rather than to a host callback. Settled by Q3 (server-side). |

If an implementation PR finds a fourth case, it STOPS and returns the question rather than adding an adapter — the plan's §3 rule 5 brief rule applies to this wave's PRs verbatim.

## 5. Owner decisions — 2026-09-15

The five Stage-A questions this gate raised were answered by the Product Lead in chat on **2026-09-15**. Quotes are verbatim RU, as given. Each decision is already written into the matrix rows it names; nothing below is a lead interpretation of an unanswered question.

### Q1 — the replay is refused or throws (row 67)

> «1. Как в Академии»

- **Was:** Academy stays on the confirmation step with the generic EARS-16 error (`apps/portal/app/verify/page.tsx:226`); the doctor routes to the sign-in door with the return-context panel (`apps/doctor/components/registration-screen.tsx:368`, owner Stage-B GO 2026-09-07 on PR #2010), recorded as an accepted divergence at `DEBT.md:152` with «this wave» as its promote-when.
- **Decision:** the Academy behaviour wins on both hosts.
- **`Allowed difference`:** `convergence: owner decision 2026-09-15 — «1. Как в Академии»`.
- **Consequence:** the doctor door-routing branch is deleted in **PR 1.7**, which promotes and closes the `DEBT.md:152` line. `registration-screen.test.tsx:368` and `register-return.spec.ts:212` are rewritten there, not deleted.

### Q2 — the cold confirmation step, and whether #1394 reverses it (rows 68, 70)

> «2. Как в Академии»

- **Was:** the Academy cold step routes to `/login` carrying the event context (`apps/portal/app/verify/page.tsx:225`, test `page.test.tsx:431`); the doctor routes to the sign-in door with the return context. #1394 (003 EARS-39) would have kept the code field and the EARS-25 resend alive instead, reversing both.
- **Decision:** the Academy behaviour wins on both hosts — the cold step routes to `/login`, context carried.
- **`Allowed difference`:** `convergence: owner decision 2026-09-15 — «2. Как в Академии»`.
- **Consequence:** **#1394 is rejected by owner decision 2026-09-15.** It produces no code in this wave; §3.7 records each of its clauses, and its volatile-password clause survives only because §3.3 already covers it independently.

### Q3 — where the signed-in decision is taken on an auth route (rows 26, 27, 37)

> «Ну очевидно, что надо убрать временный костыль и сделать полноценное перманентное серверное решение.»

- **Was:** Academy decides on the CLIENT (`apps/portal/lib/use-redirect-if-authenticated.ts:48`, wired by `components/auth-shell.tsx:41`); the doctor decides on the SERVER before paint (`apps/doctor/app/(auth)/login/page.tsx:143`).
- **Decision:** converge on the doctor mechanism — a server-side redirect before paint, inside the package's route handling.
- **`Allowed difference`:** rows 26 and 37 `convergence: owner decision 2026-09-15`; row 27 becomes `config: routes.allowAuthenticated`, because once the decision is server-side the exemption list is plain host data.
- **Consequence:** `apps/portal/lib/use-redirect-if-authenticated.ts` — the named «временный костыль» — is **deleted**, not moved, in PR 1.4/1.5. No client hook replaces it.

### Q4 — the submit while a mandatory condition is unmet (rows 16, 75)

> «Больше склоняюсь к тому, как это в Академии»

Asked together with a best-practice note from the Tech Lead — GOV.UK Design System and NN/g both hold that a submit control is never disabled to express an unmet condition, because a disabled control announces nothing to assistive technology and gives no route to the reason. The note was accepted with the quote above.

- **Was:** the Academy keeps submits ENABLED and states the reason on the attempted submit (`apps/portal/app/verify/page.tsx:244`); the doctor renders an INERT submit with its reason beside it; #1548's clause «a disabled submit that always states its reason» and the candidate #1952 primitive pointed at the doctor shape.
- **Decision:** the Academy rule on both hosts — the submit is always enabled, an attempted submit reveals every field error including the unsolved captcha, and focus moves to the first error.
- **`Allowed difference`:** `convergence: owner decision 2026-09-15 — «Больше склоняюсь к тому, как это в Академии»`.
- **Consequence:** #1548's disabled-submit clause is **rewritten** (§3.1); #1952 is not adopted by wave 1. The doctor inert-submit tests — `apps/doctor/e2e/register-layout.spec.ts:204`, `register-medworker.spec.ts:94`, `register-mobile.spec.ts:436`, `register-validation.spec.ts:162` — are rewritten to the enabled-submit rule in **PR 1.6**, and the a11y assertion they carried (the reason is announced) must survive the rewrite.

### Q5 — the doctor-only register blocks (rows 56, 57, 58)

> «Академия не для медработников — никакой декларации для них там быть не может. Про партнёрские данные тоже, так как сами партнёры там регистрируются. … всё верно — в состоянии выкл.»

- **Was:** the doctor host renders two-tier consents (`apps/doctor/app/(auth)/register/page.tsx:122`), the medical-worker declaration (`components/registration-screen.tsx:174`) and the partner-data consent item (`:477`); the Academy renders ONE required consent line (`apps/portal/lib/consent.ts:11`, rendered at `apps/portal/app/register/page.tsx:192`). The Academy's absence had never been recorded as an owner decision.
- **Decision:** the absence is a product difference of audience, not an omission — the Academy is not a medical-worker surface and partners register on their own host. All three stay **off** for the Academy, permanently.
- **`Allowed difference`:** `product divergence: host audience`, carried as `config: consents.tiers` (row 56), `config: consents.medicalWorkerDeclaration` (row 57) and `config: consents.partnerDataItem` (row 58).
- **Consequence:** rows 57 and 58 have no UNIT test on either host today; a flag-branch unit test per flag, covering both the on and the off branch, is owed in **PR 1.6** before the package can claim the difference is configured rather than accidental.

### 5.1 State of the gate after these decisions

Every one of the 85 matrix rows now carries an explained difference, and every row carries a test id or the PR that owes its test (the feed line of §2.8). No `OPEN — owner Stage-A` cell remains. Per the plan's §4 wave-entry gate, **wave 1 may open its code PRs**, starting with PR 1.1.
