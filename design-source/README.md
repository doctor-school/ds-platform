# `design-source/` — imported Claude Design canvas (single source of truth)

This folder holds the **actual design source** for the Doctor.School neo-brutalist visual language, imported verbatim from the Claude Design project **«Doctor.School визуальный язык»** (`8cc2f39a-d58e-4491-b539-4337881ced4f`).

> **Build to these files, NOT to issue-body prose.** The `.dc.html` inline styles carry the exact values — px, border widths, colors (light + dark), states, and placeholder copy. Issue descriptions are a lossy transcription and a coverage checklist, **not** the fidelity spec. Where prose and this source disagree, **the source wins**. (The neo-brutalist re-skin diverged the first time precisely because it was built from prose instead of this HTML.)
>
> **Vendor EVERY canvas the surface renders — not only the headline one.** If a demo/composition renders a unit whose spec lives in a separate canvas (a card, a block, a screen fragment), that canvas must land here BEFORE building. The #514 rhythm demo repeated the founding error one layer down: its webinar card was built from §09 prose while `ВебинарКарточка.dc.html` sat unimported.

## Files

| File | What it is |
| --- | --- |
| `design-system.dc.html` | The 9-section visual language: **01** color · **02** type · **03** spacing · **04** borders/radius/shadows · **05** components · **06** states · **07** forms & validation · **08** feedback · **09** layout & rhythm. Exact token values are in the `renderVals()` block at the bottom (light `d=false` / dark `d=true`). |
| `webinar-card.dc.html` | The webinar card (canvas `ВебинарКарточка.dc.html`): desktop grid `196px 1fr` + 2px border + `6px 6px 0` blue shadow, time 56px, body pad 30/32; **mobile ≤900 — flat**: `border:0`, `box-shadow:none`, full-bleed, time 40px, body pad 16, CTA shadows off; variants live/scheduled/registered/past (live sticker rotate 3deg desktop / inline plate mobile). |
| `webinars-listing.dc.html` | The webinar listing screen (canvas `Эфиры.dc.html`): the §09 rhythm applied for real — desktop day header = label + 2px ink rule (margin 48/24), card list `gap:28px`; **mobile** day header = full-bleed `section`-bg band (`margin:0 -16px`), card list `gap:0; margin:0 -16px` (cards bleed, separation via the tint time plate — no borders at all). |
| `auth.dc.html` | The auth flow (canvas `Авторизация.dc.html`): 4 screens — login (password/OTP tabs, email-or-phone identity), register, verify (8-slot OTP focus-screen / 6-slot inline), reset (request + 6-slot complete stage); split-screen desktop (`.95fr 1.05fr`, brand panel `order:1`) collapses to single column ≤900px (brand panel hidden); light/dark via `localStorage['ds-theme']`; client-side email/phone/password validators inline in `renderVals()`. |
| `webinar-page.dc.html` | The public event page (canvas `Вебинар.dc.html`): blue poster header (breadcrumbs, uppercase school kicker, `clamp(30px,5.5vw,50px)` H1, specialty chips `2px #6BB1F7`, rotated status badge) with the status card pulled up `-80px` over it, reusing the webinar-card geometry (desktop `196px 1fr` + 2px border + `6px 6px 0` shadow, time 56px; **mobile ≤900 — flat full-bleed**, time 40px); **3 lifecycle states** via the `status` prop enum `upcoming\|live\|ended` (hero badge, time plate, CTA pair, footer CTA all swap); body = two columns `1fr 380px` (program rows `96px 1fr`, sponsor plate, speaker cards `4px 4px 0`), bottom blue CTA band. |
| `webinar-room.dc.html` | The live room (canvas `Комната эфира.dc.html`): slim blue header with pulsing `В эфире` chip + viewer count; **desktop** grid `1fr 400px` — 16/9 dark player (2px border + `6px 6px 0` shadow, Live/HD chips, gradient control bar) beside a boxed chat aside (Чат/Вопросы tabs, pinned tint moderator note, speaker-badged messages) + current-program strip `01/03`; **mobile** — full-bleed 16/9 player, slim title bar, 3 tabs `Чат / Вопросы / О эфире` (questions with ▲ vote counts, program list). |
| `my-events.dc.html` | The personal «мои события» cabinet (canvas `Мои события.dc.html`): blue poster header + 3 tabs `Предстоящие / Записи / Сертификаты` (desktop boxed tab bar `6px 6px 0`; mobile full-bleed 3-col grid); specialty («направление») filter — desktop chip row / mobile dropdown, with reset + empty-filter state; day-grouped upcoming list renders `ВебинарКарточка` via `dc-import` (variants live/registered/past, §09 rhythm: desktop label + 2px rule, mobile full-bleed band); recordings list + certificate-card grid `minmax(260px,1fr)`. |
| `profile.dc.html` | The account/profile surface (canvas `Профиль.dc.html`): **3 composition variants** via prop `variant` enum `Анкета\|Разделы\|Визитка` (**canvas default: «Разделы»**) + `phoneFilled` bool; blue poster header («Профиль» H1 + подзаголовок), content column `max-width:720px`; rows = desktop grid `150px 1fr auto` / mobile ≤900 stacked (`1fr auto`, label full-width); name row: 44px avatar initials plate + inline edit (input + Сохранить/Отмена, Enter/Escape), email + green `✓ подтверждён` outline badge, phone or muted «не указан», «Сменить пароль» row → reset flow (helper «Отправим ссылку для сброса на email»), «Мои события» link row, red «Выйти из аккаунта»; «Анкета» wraps rows in the 2px-border/`6px 6px 0` shadow card (mobile full-bleed flat), «Разделы» = flat rows under §09 section headers (label + 2px rule), «Визитка» = name/email in the blue masthead with 88/64px avatar. |
| `webinar-room-v2.dc.html` | The room-redesign gallery (canvas `Комната эфира v2.dc.html`): the artboard index for the Twitch-model rework — desktop 1440×840 (light/dark × chat open/collapsed), mobile 390×760 (Чат/О эфире × light/dark), the 8 chat-state frames at 342×520, and the player-unavailable frame. Composes the two component canvases below via `dc-import`; carries no geometry of its own. |
| `webinar-room-frame.dc.html` | The room frame (canvas `КомнатаКадр.dc.html`, **supersedes `webinar-room.dc.html` for #1123+**): viewport-height column, page never scrolls (`overflow:hidden`). **Desktop** — slim blue header (live pill «В эфире · N мин», «N врачей в комнате», exit link, avatar plate), then `flex:1` row: player area `flex:1` (dark `oklch(.13 .018 250)`, Live/HD chips, gradient control bar; unavailable state with «Обновить страницу») + one-line context strip (school kicker · title · speakers, 12px/20px pad) beside the 340px chat column (44px collapsed). **Mobile** — slim header, full-bleed 16/9 player, one-line «В эфире» strip, 2-tab `Чат / О эфире` (info tab: title, speakers, «Материалы эфира», program list). Props: `theme`, `mobile`, `collapsed`, `tab`, `chatState` (7-value enum), `playerState`. |
| `chat-column.dc.html` | The chat column (canvas `ЧатКолонка.dc.html`): Twitch-minimal ledger — messages are borderless single paragraphs (13px/1.5), **bold name + text inline, no timestamps/avatars**; name color = ink, accent for speaker/own; tint badges `Спикер`/`Мод`; pinned moderator plate; ledger is `column-reverse` `overflow-y:auto` with stick-to-bottom (`scrollTop≈0`) + «Новые сообщения ↓» chip when unstuck; header row «Чат эфира · N» with `»` collapse button; collapsed = 44px rail (« button, vertical label, red unread count); states: empty/loading-skeleton/reconnecting (dimmed ledger + ⟳ strip, composer disabled)/lost/senderror («Не отправлено · Повторить»)/unavailable (composer hidden); composer = hairline input + 40px `↑` send. |
| `webinars-month.dc.html` | The month-calendar listing (canvas `Эфиры месяц.dc.html`): month-picker dropdown (12-month grid with per-month event counts) + `‹ ›` paging + `Неделя / Месяц` view switcher; **desktop** 7-col grid (`min-width:840px`, h-scroll), cells `min-height:118px` with tint event pills / red live pill / muted past notes, today = 3px inset outline; **mobile** compact dot-grid calendar (≤3 dots per day, red = live) + selected-day agenda rows; legend live/scheduled/past. |

## Which issues build against this

- **DS foundation** — `#510` (parent) → `#512` core primitives · `#513` new primitives · `#514` layout & rhythm · `#515` showcase/a11y capstone → **`design-system.dc.html`** (all sections; component geometry in §05–§08, layout in §09).
- **Auth re-skin** — `#516`–`#520` → `Авторизация.dc.html` *(loaded here at the start of the auth phase).*
- **Webinars** — `#471` (features 004–007, mockup refs in each `NNN-product.md` → *Approved mockup*) → `Эфиры.dc.html` (**vendored: `webinars-listing.dc.html`**), `ВебинарКарточка.dc.html` (**vendored: `webinar-card.dc.html`** — pulled early for the #514 rhythm demo fidelity), `Вебинар.dc.html` (**vendored: `webinar-page.dc.html`**), `Комната эфира.dc.html` (**vendored: `webinar-room.dc.html`**), `Мои события.dc.html` (**vendored: `my-events.dc.html`**), `Эфиры месяц.dc.html` (**vendored: `webinars-month.dc.html`**). Feature 007 (admin) has **no canvas** — the neo-brutalist set is portal-facing; admin is Refine (`admin` app, ADR-0004).
- **Account profile** — `#770` (spec 003 EARS-27/28, PR #813) → `Профиль.dc.html` (**vendored: `profile.dc.html`**). Build the **«Разделы»** composition variant (the canvas default); the header/footer chrome in the canvas is context, not part of the #770 surface.
- **Webinar-room Twitch-model rework** — `#1123` (independent chat scroll / maximized player / collapsible minimal chat) → `Комната эфира v2.dc.html` (**vendored: `webinar-room-v2.dc.html`**) composing `КомнатаКадр.dc.html` (**vendored: `webinar-room-frame.dc.html`**) + `ЧатКолонка.dc.html` (**vendored: `chat-column.dc.html`**). For the room these supersede `webinar-room.dc.html` (kept as the pre-rework reference). The ledger names in the canvas presume the `#1121` display-names decision; until #1121 ships the ledger renders the current author labels with the SAME row anatomy.
- Not vendored, by design: `Адаптив-превью.dc.html` is a utility preview canvas (the only cross-canvas unit the vendored webinar screens `dc-import` is `ВебинарКарточка`, already here); `Направления.dc.html` is a design-history canvas — the «three directions of the visual language» exploration that preceded `design-system.dc.html` — not a build reference (the specialty/«направление» filter is a control inside the listing/«мои события» canvases, not a separate screen).

## Provenance & how to refresh

- **Project:** <https://claude.ai/design/p/8cc2f39a-d58e-4491-b539-4337881ced4f>
- **This file:** <https://claude.ai/design/p/8cc2f39a-d58e-4491-b539-4337881ced4f?file=%D0%94%D0%B8%D0%B7%D0%B0%D0%B9%D0%BD-%D1%81%D0%B8%D1%81%D1%82%D0%B5%D0%BC%D0%B0.dc.html>
- **Re-pull the exact bytes:** `DesignSync get_file` against project `8cc2f39a` (canvas filename `Дизайн-система.dc.html` — renamed to ASCII `design-system.dc.html` here for cross-platform/tooling safety; content is byte-identical).
- **View it live:** open the Claude Design link above. The DC runtime (`support.js`) that renders the `<x-dc>` template is not vendored here — re-pull it via `DesignSync get_file` on `support.js` if you ever need to render locally (it expects `window.React` / `window.ReactDOM` globals).

## Notes

- The canvas references logo assets `assets/ds-logo-{color,icon,white}.svg`. Those live in the Claude Design project; the repository's own brand marks are at `apps/portal/public/brand/`. They are intentionally not duplicated here — this folder is a **spec reference**, not a runnable app.
- Per ADR-0014 §4 the canvas is *source*, the repo holds the *built artifact* (the `@ds/design-system` React components). This folder is the pinned build reference the components are verified against; it is not itself shipped.
- This folder is `.prettierignore`d so the imported bytes stay verbatim.
- The lychee link-check ignores everything under this folder (`.lycheeignore` → `^file://.*/design-source/.*$`): canvas `{{ template }}` hrefs and cross-canvas `.dc.html` links resolve only inside the Claude Design project. Vendoring a new canvas needs **no** extra lychee work.
