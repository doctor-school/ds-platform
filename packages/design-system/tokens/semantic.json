{
  "color": {
    "$type": "color",
    "$description": "Semantic roles for the neo-brutalist visual language over the Doctor School brand. Light-first; the .dark overrides live in semantic.dark.json. Brand primary = blue.500 #2D84F2 stays the brand ANCHOR (link accents, icons, tints, focus). White-on-fill AA (ADR-0013 §7): white on blue.500 is only 3.69:1, so a NORMAL-weight filled action does NOT paint blue.500 — it uses the accessible action-fill triad `primary-action` (blue.700, white 8.14:1, resting), `primary-hover`/`primary-pressed` (blue.800, 11.12:1), which keeps a visible resting→hover delta while clearing AA on every state. The same blue.700 rule governs any text-bearing brand FILL: `primary-surface` (blue.700) is the brand fill for a panel carrying normal-weight white copy. Text tiers: `foreground` ink / `muted-foreground` / `faint` / `muted-2` (neutral 900/600/500/400). Frame: structural `border` is the hard near-black outline (neutral.900), `hairline` (neutral.200) the subtle divider. Status = success green, warning amber (dark ink — white fails AA on amber), danger `destructive` red. `header` is the brand chrome band. `tint` is the pale-blue info surface. `elevation`/`elevation-soft` are the theme-aware cast-shadow tones the offset shadows reference.",
    "background": {
      "$value": "{color.white}",
      "$description": "The default page/app surface (neo-brutalist page = flat white). Pair with `foreground` for text."
    },
    "foreground": {
      "$value": "{color.neutral.900}",
      "$description": "Ink — the default text and icon colour, used on `background`."
    },
    "section": {
      "$value": "{color.neutral.150}",
      "$description": "A faintly-tinted block surface used to band a section apart from the flat page, without a card border."
    },
    "card": {
      "$value": "{color.white}",
      "$description": "A raised surface (card, popover, panel) sitting on `background`, framed by the hard `border`. Pair with `card-foreground`."
    },
    "card-foreground": {
      "$value": "{color.neutral.900}",
      "$description": "Text and icon colour on a `card` surface."
    },
    "primary": {
      "$value": "{color.blue.500}",
      "$description": "Brand anchor (blue.500) for link accents, focus, icons and tints. NOT a normal-weight filled action — that fails AA; use `primary-action`."
    },
    "primary-foreground": {
      "$value": "{color.white}",
      "$description": "Text and icon colour ON a primary/action fill (white on the brand button)."
    },
    "primary-action": {
      "$value": "{color.blue.700}",
      "$description": "Resting fill for a normal-weight filled primary button (blue.700 = #114D9E, white 8.14:1). Pairs with `primary-foreground`. Also the AA link-text colour."
    },
    "primary-hover": {
      "$value": "{color.blue.800}",
      "$description": "Hover fill for a primary action (darker than the resting `primary-action`)."
    },
    "primary-pressed": {
      "$value": "{color.blue.800}",
      "$description": "Pressed/active fill for a primary action."
    },
    "primary-surface": {
      "$value": "{color.blue.700}",
      "$description": "Brand-coloured panel/surface that carries normal-weight white copy (e.g. the auth brand panel)."
    },
    "secondary": {
      "$value": "{color.neutral.100}",
      "$description": "Low-emphasis tonal fill (secondary buttons, subtle chips). Pair with `secondary-foreground`."
    },
    "secondary-foreground": {
      "$value": "{color.neutral.900}",
      "$description": "Text and icon colour on a `secondary` fill."
    },
    "muted": {
      "$value": "{color.neutral.100}",
      "$description": "A muted background for subtle fills, hovered rows or disabled tracks."
    },
    "muted-foreground": {
      "$value": "{color.neutral.600}",
      "$description": "De-emphasised text — helper text, placeholders, captions, metadata (neutral.600)."
    },
    "faint": {
      "$value": "{color.neutral.500}",
      "$description": "Third text tier below `muted-foreground` — timestamps, tertiary metadata (neutral.500)."
    },
    "muted-2": {
      "$value": "{color.neutral.400}",
      "$description": "Faintest text tier — disabled labels, decorative captions (neutral.400). Non-body use only; below the AA body-text threshold by design."
    },
    "accent": {
      "$value": "{color.neutral.100}",
      "$description": "Hover/selected background for interactive list items and ghost buttons. Pair with `accent-foreground`."
    },
    "accent-foreground": {
      "$value": "{color.neutral.900}",
      "$description": "Text and icon colour on an `accent` surface."
    },
    "tint": {
      "$value": "{color.blue.100}",
      "$description": "Pale-blue informational surface (tint chips, callouts). Pair with `tint-foreground`."
    },
    "tint-foreground": {
      "$value": "{color.blue.700}",
      "$description": "Text and icon colour on a `tint` surface (blue.700 on blue.100, 6.49:1 AA)."
    },
    "header": {
      "$value": "{color.blue.500}",
      "$description": "Brand chrome band (top nav / app header) — the brand-forward blue.500. Pair with `header-foreground`; header text is large/bold brand or logo (white on blue.500 is 3.69:1, the large/bold ≥3:1 carve-out, not normal-weight body — see AA note in ADR-0013 §7)."
    },
    "header-foreground": {
      "$value": "{color.white}",
      "$description": "Text, logo and icon colour on the `header` band (white)."
    },
    "success": {
      "$value": "{color.green.500}",
      "$description": "Positive-status fill, border or icon. Pair with `success-foreground` (white is large/bold on green.500 = 3.68:1)."
    },
    "success-foreground": {
      "$value": "{color.white}",
      "$description": "Text and icon colour on a `success` fill."
    },
    "warning": {
      "$value": "{color.amber.500}",
      "$description": "Caution-status fill, border or icon (functional amber). Pair with `warning-foreground` — white FAILS AA on amber, so the foreground is dark ink."
    },
    "warning-foreground": {
      "$value": "{color.neutral.950}",
      "$description": "Dark ink on a `warning` amber fill (neutral.950, 5.14:1 on amber.500 / 8.99:1 on amber.400 dark)."
    },
    "destructive": {
      "$value": "{color.red.500}",
      "$description": "Error/danger/live fill, border or icon — destructive actions, invalid state, live indicators. Pair with `destructive-foreground`."
    },
    "destructive-foreground": {
      "$value": "{color.white}",
      "$description": "Text and icon colour on a `destructive` fill."
    },
    "border": {
      "$value": "{color.neutral.900}",
      "$description": "Structural border — the hard near-black outline (neutral.900) of the neo-brutalist frame (2px). Not the subtle divider; that is `hairline`."
    },
    "hairline": {
      "$value": "{color.neutral.200}",
      "$description": "Subtle 1px divider/rule between rows and list items, where a hard structural `border` would be too heavy."
    },
    "input": {
      "$value": "{color.neutral.900}",
      "$description": "Border colour of form inputs and controls — the same hard structural outline as `border`."
    },
    "ring": {
      "$value": "{color.blue.300}",
      "$description": "The keyboard-focus ring colour (blue.300); the `focus` shadow (`shadow.focus`) is blue.300 @ 50%."
    },
    "elevation": {
      "$value": "{color.blue.700}",
      "$description": "Primary cast-shadow tone for the hard offset shadows (`shadow.md`/`lg`). Theme-aware: blue.700 light → blue.300 dark."
    },
    "elevation-soft": {
      "$value": "{color.blue.200}",
      "$description": "Softer cast-shadow tone for the small offset shadows (`shadow.sm`/`base`). Theme-aware: blue.200 light → blue.600 dark."
    }
  },

  "radius": {
    "$type": "dimension",
    "control": { "$value": "{radius.base}" }
  },

  "text": {
    "body": {
      "font-size": { "$type": "dimension", "$value": "{font.size.base}" },
      "line-height": {
        "$type": "number",
        "$value": "{font.line-height.normal}"
      },
      "font-weight": { "$type": "fontWeight", "$value": "{font.weight.normal}" }
    },
    "heading": {
      "font-size": { "$type": "dimension", "$value": "{font.size.2xl}" },
      "line-height": {
        "$type": "number",
        "$value": "{font.line-height.tight}"
      },
      "font-weight": {
        "$type": "fontWeight",
        "$value": "{font.weight.semibold}"
      }
    },
    "label": {
      "font-size": { "$type": "dimension", "$value": "{font.size.sm}" },
      "line-height": {
        "$type": "number",
        "$value": "{font.line-height.normal}"
      },
      "font-weight": { "$type": "fontWeight", "$value": "{font.weight.medium}" }
    },
    "micro-label": {
      "$description": "Neo-brutalist micro-label / eyebrow: 10px extrabold, wide +.12em tracking, UPPERCASE. Section eyebrows, chip labels, meta tags.",
      "font-size": { "$type": "dimension", "$value": "{font.size.2xs}" },
      "line-height": { "$type": "number", "$value": "{font.line-height.none}" },
      "font-weight": {
        "$type": "fontWeight",
        "$value": "{font.weight.extrabold}"
      },
      "letter-spacing": {
        "$type": "dimension",
        "$value": "{font.letter-spacing.micro}"
      },
      "text-transform": { "$value": "uppercase" }
    }
  },

  "motion": {
    "duration": {
      "$type": "duration",
      "fast": { "$value": "{duration.fast}" },
      "base": { "$value": "{duration.base}" }
    },
    "easing": {
      "$type": "cubicBezier",
      "standard": { "$value": "{easing.standard}" }
    }
  }
}
