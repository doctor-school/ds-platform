{
  "name": "@ds/eslint-rules-local",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Local flat-config ESLint plugin for DS Platform repo-specific lint gates (auth field-primitive enforcement, #197). Plain ESM so the root `eslint .` path needs no build/tsx step.",
  "main": "./index.mjs",
  "exports": {
    ".": "./index.mjs"
  },
  "scripts": {
    "test": "vitest run"
  },
  "devDependencies": {
    "@typescript-eslint/parser": "^8.63.0",
    "eslint": "^10.7.0",
    "typescript-eslint": "^8.63.0",
    "vitest": "^4.1.10"
  }
}
