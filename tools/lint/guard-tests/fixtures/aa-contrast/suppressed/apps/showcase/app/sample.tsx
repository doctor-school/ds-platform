/* aa-contrast-ok: this specimen deliberately demonstrates the dimmed-foreground
   anti-pattern in a contrast-education panel — a genuine, reviewed exception. */
export function Sample() {
  return <p className="text-sm text-muted-foreground/70">Intentional demo.</p>;
}
