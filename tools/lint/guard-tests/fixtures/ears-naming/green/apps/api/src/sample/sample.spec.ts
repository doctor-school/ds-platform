// All canonical EARS shapes + legit non-EARS titles → guard passes (exit 0).
describe("sample", () => {
  it("EARS-2: when a user registers, the system shall create an account", () => {});
  it("EARS-3.1: when the handler upserts, it shall persist the profile", () => {});
  it("EARS-25/16: an unknown identifier yields the identical enumeration-safe ack", () => {});
  it("EARS-2 (#202): a phone-only register is a handled enumeration-safe failure", () => {});
  it("#147: maps a policy error to a generic 422 (issue-regression, not EARS)", () => {});
  it("maps a 409 duplicate to alreadyExisted (a plain adapter unit test)", () => {});
});
