describe("health", () => {
  // EARS-18 must NOT fold into the spec's EARS-1 (component-wise, `1` is not a
  // prefix of `18`): EARS-1 stays uncovered AND EARS-18 stays an orphan.
  it("EARS-18: cites an unrelated flat id", () => {});
});
