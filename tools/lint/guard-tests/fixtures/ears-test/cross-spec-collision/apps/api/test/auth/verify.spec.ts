// 003 auth verify test — carries NO feature-number prefix, so its EARS ids are
// feature-agnostic (they match any spec's same-numbered id, as the guard always
// has). Covers 003 EARS-3; deliberately does NOT cite EARS-4 (that is 003's
// deferred, unbuilt phone-verify path).
describe("verify (e2e)", () => {
  it("EARS-3: verifies the email code", () => {});
});
