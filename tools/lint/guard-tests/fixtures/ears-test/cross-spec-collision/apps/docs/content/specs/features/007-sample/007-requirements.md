# 007 sample requirements (owns a SEPARATE EARS-4 — the publish transition)

- EARS-4: when an operator publishes a draft event, the system shall transition it
  draft → published. This is a DIFFERENT requirement from 003's EARS-4 (flat
  numbering is per-spec, ADR-0006 §4).
