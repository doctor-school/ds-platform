describe("placeholder", () => {
  // No test cites EARS-7 — it is an accepted deferral tracked via the allowlist
  // (the guard-test injects LINT_EARS_DEFERRALS to point EARS-7 at a fixture issue).
  it("unrelated unit assertion (no EARS id)", () => {});
});
