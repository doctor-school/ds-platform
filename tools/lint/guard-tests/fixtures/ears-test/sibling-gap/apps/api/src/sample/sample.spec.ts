describe("oidc callback", () => {
  it("EARS-3.1: upserts the doctor profile", () => {});
  // EARS-3.2 (emit DoctorRegistered) has NO test — a sibling nested id does not
  // cover it, so this must surface as an uncovered-requirement finding.
});
