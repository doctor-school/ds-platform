---
title: Well-formed frontmatter in an MDX page
prior_decisions:
  - "ADR-0001 — quoted entry with `access: authenticated` inside"
---

# MDX body

A well-formed `.mdx` frontmatter block — counted by the guard's scan alongside
the `.md` files.
