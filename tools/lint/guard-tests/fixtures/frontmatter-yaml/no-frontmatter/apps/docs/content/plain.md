# A plain markdown file

This file has no `---` frontmatter block at all, so there is no frontmatter YAML
to parse. A horizontal rule mid-document is not frontmatter:

---

That rule must not be mistaken for a frontmatter delimiter.
