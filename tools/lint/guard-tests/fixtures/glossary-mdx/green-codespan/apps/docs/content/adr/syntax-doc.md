# Documenting the glossary syntax

The directive `[[unknown_inline]]` is written as inline code to SHOW the syntax —
it is not a live reference. A fenced example:

```md
Use [[another_unknown]] to reference a glossary term.
```

Both are masked before matching, so neither is a finding.
