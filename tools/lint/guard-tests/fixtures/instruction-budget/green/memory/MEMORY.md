# MEMORY.md (fixture — within budget)

Plain-text index lines, comfortably under the 200-line / 25 KB cutoff. No
markdown link syntax: fixture files are scanned by the repo-wide markdown-links
(lychee) CI job, so a fake broken link would fail it.

- topic one: short index line, under budget.
- topic two: short index line, under budget.
