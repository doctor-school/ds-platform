/** RED (#1874): `apps/doctor/lib` also holds authored TSX on this host. */
export function renderCta() {
  return (
    <a href="/register" className="hover:underline focus-visible:shadow-focus">
      Регистрация
    </a>
  );
}
