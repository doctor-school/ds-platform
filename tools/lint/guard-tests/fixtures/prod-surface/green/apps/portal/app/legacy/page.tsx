export default function LegacyPage() {
  // This route is classified `deferred` (tracked by #123) in the manifest, so
  // the scaffold tell below is exempt from Part 2 — the manifest IS the
  // suppression mechanism.
  return <p>Каркас приложения — страница собирается на общей дизайн-системе.</p>;
}
