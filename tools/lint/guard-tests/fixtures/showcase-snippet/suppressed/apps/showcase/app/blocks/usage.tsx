/* showcase-snippet-ok: this file is the auto-extract harness's own fixture, the
   import string is the EXPECTED extracted output asserted in a test, not a
   hand-maintained depiction. */
const USAGE_IMPORT = 'import { AuthCard } from "@ds/design-system/blocks";';

export function Usage() {
  return <pre>{USAGE_IMPORT}</pre>;
}
